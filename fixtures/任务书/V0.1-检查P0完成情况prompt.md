你现在的角色不是“评论者”或“方案顾问”，而是 **资深技术负责人 + Staff Engineer + 架构师 + 交付经理 + 评测负责人**。  
你的任务不是继续写泛泛的架构分析，也不是复述已有分析结论，而是输出一份 **可直接交给 Codex 执行的工程任务书**。

---

# 关键前提（务必遵守）

你**看不到我本地文件系统**，也**不能直接检查我电脑上的工作区**。  
你本次可使用的事实来源只有两类：

1. GitHub 仓库地址：  
   `https://github.com/watsonk1998/008-review-control-plane`

2. 一份已有分析文档：  
   `V0.1-分析2.md`

请严格理解以下边界：

- **GitHub 仓库是第一事实源**
- `V0.1-分析2.md` 是第二事实源
- 你不能假设能读取我本机目录内容
- 你不能把“本地工作区里可能存在的内容”写成已确认事实
- 你必须在任务书中明确写出：  
  **这份任务书将交给具有本地文件操作权限的 Codex 执行，目标本地工作区路径是：**  
  `/Users/lucas/repos/review/008-review-control-plane`

也就是说：

- 你写任务书时，**事实判断必须基于 GitHub 仓库与 `V0.1-分析2.md`**
- 你写任务书时，**执行落地路径必须明确写成 Codex 将在本地目录**  
  `/Users/lucas/repos/review/008-review-control-plane`  
  **进行修改**

如果 GitHub 仓库与 `V0.1-分析2.md` 不一致，**一律以 GitHub 仓库为准**。

---

# 输入材料优先级

请严格按以下优先级吸收信息：

1. **第一事实源：GitHub 仓库**
   - `https://github.com/watsonk1998/008-review-control-plane`
2. **第二事实源：V0.1-分析2.md**
3. **第三参考：仓库中的 README / docs / tests / fixtures / evaluation 相关说明**
4. **最低优先级：你自己的泛化经验**

如果出现冲突，必须在任务书中显式区分：
- 哪些是 GitHub 仓库已确认事实
- 哪些是 `V0.1-分析2.md` 中的判断
- 哪些是你基于现状提出的建议改造项

---

# 本次任务的目标

请输出一份完整 Markdown 文档，标题固定为：

# 008-review-control-plane P0 完成度检查 + 未完成项/P1 改造任务书（Codex 执行版）

这份任务书的用途是：

- 先基于 GitHub 仓库**独立检查 P0 是否完成、完成到什么程度**
- 不允许直接照抄或默认接受 `V0.1-分析2.md` 的判断
- 必须由你自己基于 GitHub 仓库进行完成度判定
- 然后把：
  1. **P0 未完成部分**
  2. **原本属于 P1 的必要工作**
  
  合并成一份新的、可直接交给 Codex 的执行任务书

最终我会把你输出的任务书交给 Codex，Codex 将在本地目录：

`/Users/lucas/repos/review/008-review-control-plane`

中执行改造。

---

# 你必须先做的第一件事：独立检查 P0 完成情况

这是本次任务最重要的要求：

**你必须先自己检查 GitHub 仓库当前状态，判断 P0 做到了多少、哪些已经完成、哪些没有完成。**  
**不要把外部已有分析结论直接当成答案。**

尤其注意：

- 不要使用“已有分析认为……”来替代你的判断
- 不要把“我给你的分析2里的结论”直接搬进结果
- 你必须自己基于 GitHub 仓库做事实盘点，再得出 P0 完成度结论

---

# 本次对 P0 的检查口径（必须采用）

你必须用以下三组口径检查 P0：

## P0-1：双轨边界是否已经做实
检查至少包括：
- `review_assist` 是否仍保留
- `structured_review` 是否已成为独立 task type / 独立执行路径
- runtime / router / planner 是否真的把两者分开
- 是否仍存在职责串扰、复用过深、边界不清的问题
- 是否还有 formal review 逻辑继续堆在通用 runtime 中而没有下沉

## P0-2：输入解析与可视域语义是否已经做实
检查至少包括：
- document loader / parser 是否保留章节 / 表格 / 附件 / figure / appendix 等对象
- 是否已经有 `visibility` / `attachment_unparsed` / `manualReviewNeeded` 等语义
- 是否仍存在“系统没看到 = 文档缺失”的误判风险
- parser / visibility contract 是否一致
- runtime / report / UI / artifact 是否完整消费这些语义
- 是否还存在“为了绕过问题直接关闭 visibility check”的路径

## P0-3：最小规则核与结构化闭环是否已经做实
检查至少包括：
- 最小支持文档类型是否真的能跑通
- facts / rules / evidence / final issues / report / matrices / artifacts 是否贯通
- 是否已经有最小规则核覆盖：
  - 施组结构完整性
  - 危大识别
  - 重大隐患初筛
  - 附件可视域
  - 应急预案针对性
  - 工期 / 资源基础冲突
- tests / fixtures / eval 是否已形成最小回归闭环
- 哪些仍只是骨架、placeholder、seed data、未 fully wired 的接口

---

# 你必须先完成的 GitHub 仓库盘点动作

在写任务书之前，你必须先基于 GitHub 仓库真实内容做盘点，并在任务书中反映盘点结论。你必须至少检查并判断以下对象是否已经存在、成熟度如何、是否属于 P0 已完成项或未完成项：

## 后端与 domain / runtime
- `apps/api/src/domain/models.py`
- `apps/api/src/routes/tasks.py`
- `apps/api/src/services/sqlite_store.py`
- `apps/api/src/services/document_loader.py`
- `apps/api/src/orchestrator/planner.py`
- `apps/api/src/orchestrator/router.py`
- `apps/api/src/orchestrator/deepresearch_runtime.py`

## review 子域
- `apps/api/src/review/schema.py`
- `apps/api/src/review/pipeline.py`
- `apps/api/src/review/parser/*`
- `apps/api/src/review/extractors/*`
- `apps/api/src/review/rules/*`
- `apps/api/src/review/evidence/*`
- `apps/api/src/review/report/*`
- `apps/api/src/review/evaluation/*`

## 前端
- `apps/web/src/types/control-plane.ts`
- `apps/web/src/components/home-dashboard.tsx`
- `apps/web/src/components/structured-review-form.tsx`
- `apps/web/src/components/task-detail.tsx`

## 文档、样本、评测与测试
- `README.md`
- `DELIVERY_REPORT.md`
- `docs/architecture.md`
- `docs/formal-review.md`
- `docs/testing.md`
- `docs/known-limitations.md`
- `fixtures/review_eval/*`
- `tests/test_structured_review.py`
- `tests/test_runtime.py`
- `tests/test_task_service_and_routes.py`
- 其他与 structured_review / review_assist / artifacts / eval 直接相关的真实文件

---

# 你必须先回答的关键问题（然后再出任务书）

在任务书的“P0 完成度检查”部分，你必须先基于 GitHub 仓库明确回答：

1. 当前 GitHub 仓库是否已经存在：
   - `structured_review` task type
   - review 子域目录
   - review schema
   - pipeline
   - parser / extractors / rules / evidence / report
   - artifact contract / artifact API
   - structured_review 前端入口与结果渲染
   - tests / fixtures / eval harness

2. 这些能力分别处于什么成熟度：
   - 已实现
   - 初步可用
   - 有骨架
   - 仅 docs / DTO
   - 尚未落地

3. 当前 P0 最真实的未完成项是什么：
   - runtime / router / planner 的边界不稳？
   - parser / visibility 语义不统一？
   - facts / rule hits / final issues 契约不贯通？
   - evidence / artifact / UI contract 有折损？
   - eval / golden / regression 未接好？
   - 仍存在 fixture-first 假设？
   - 仍存在字段命名重复 / 并存 / 漂移？
   - 仍存在“关闭 visibility check 规避问题”的路径？

4. 哪些事情其实已经做了，不能再被误写成“后续新增项”？

5. 哪些东西虽然已经存在，但只是“骨架 / 最小原型”，必须纳入这次新的任务书中继续做实？

---

# 你必须吸收并转译的核心判断

你必须把以下判断转译成工程任务，而不是只复述：

1. 当前 GitHub 仓库应被视为一个  
   **review control plane + structured_review 最小可用原型骨架**

2. 本次不是“从零做 formal review”，而是：
   - **先独立检查 P0 完成度**
   - **再把 P0 未完成项 + P1 必要项合并成新的改造任务书**

3. 新任务书不追求：
   - 全行业 / 全场景一步到位
   - 全 pack 覆盖
   - 一次性做完所有 reviewer workflow
   - 大规模多模态平台
   - 用更大模型掩盖 contract 问题

4. 新任务书真正追求的是：
   - 把 P0 收尾做实
   - 把最必要的 P1 合并进来
   - 形成一个能继续推进的、边界清晰、可测、可回归的下一阶段施工清单

5. 新任务书完成后，Codex 至少应能在本地目录  
   `/Users/lucas/repos/review/008-review-control-plane`  
   中继续推进：
   - P0 收尾项
   - P1 中最必要的架构、contract、rule、eval、UI、artifact 改造

---

# 你必须遵守的写作纪律

## 1）GitHub 仓库不是空白系统
不要把当前系统写成“还没有 structured_review / 还没有 review 子域 / 还没有 formal review 骨架”的空白状态。  
你必须先核对 GitHub 仓库，再判断哪些已完成、哪些未完成。

## 2）不要把分析2的结论当成你的检查结果
`V0.1-分析2.md` 只能作为辅助参考。  
你必须自己检查 GitHub 仓库，并独立得出 P0 完成情况。

## 3）不要写空话
禁止大量使用下列表达而不落地：
- 建议优化
- 建议增强
- 建议完善
- 可进一步研究
- 后续可以探索

每个建议都必须落到：
- 哪个文件
- 哪个模块
- 改什么
- 为什么改
- 如何验收

## 4）不要脱离 GitHub 仓库写理想系统
不要写一个与当前 GitHub 仓库结构无关的“理想架构”。  
必须绑定到当前真实目录、真实模块、真实 contract、真实 tests。

## 5）必须控制新增复杂度
即使要把 P1 的必要项合并进来，也必须说明：
- 为什么这是 P0 未完成后的自然延伸
- 为什么这项 P1 工作现在必须做
- 为什么它不是“未来再说”的纯扩张项

## 6）必须防止过拟合
任务书必须专门写出不能做的 case-specific patch，例如：
- 看到“煤气区域”就固定报某条
- 看到“汽车吊”就固定报某条
- 看到某个冷轧厂表达就硬编码判断

你必须强调：
- 规则要可复用
- 不允许单样本打补丁
- 不允许用样本文风伪装 formal review 能力

## 7）必须区分三类内容
在整份任务书中显式区分：
- **已确认事实（GitHub 仓库证据）**
- **合理推断**
- **建议改造项**

不得把推断写成既成事实。

## 8）必须写清楚执行落地路径
你不能假设能读取本地目录，但必须在任务书中明确说明：

- **本任务书基于 GitHub 仓库与 `V0.1-分析2.md` 生成**
- **任务书落地执行者是 Codex**
- **Codex 将在本地路径 `/Users/lucas/repos/review/008-review-control-plane` 中执行改造**

这条必须明确写进任务书的“背景 / 适用范围 / 执行说明”部分。

---

# 你输出的任务书必须包含的章节（不得省略）

## A. 执行摘要
要求：
- 300~600 字
- 说明本次任务书的目标：  
  先检查 P0 完成情况，再输出“P0 未完成项 + P1 必要项”的合并任务书
- 明确说明：事实源是 GitHub 仓库与 `V0.1-分析2.md`
- 明确说明：执行将在本地路径 `/Users/lucas/repos/review/008-review-control-plane` 由 Codex 落地

## B. P0 完成度检查（必须先写）
要求：
这是整份文档的第一核心部分。  
必须先对 P0 做独立检查，至少包括：

### B1. P0 检查口径说明
- 你如何定义 P0
- 你用什么标准检查 P0-1 / P0-2 / P0-3

### B2. P0-1 双轨边界完成情况
- 已完成什么
- 未完成什么
- 当前成熟度如何
- 证据来自哪些 GitHub 文件

### B3. P0-2 输入解析 / 可视域完成情况
- 已完成什么
- 未完成什么
- 当前成熟度如何
- 证据来自哪些 GitHub 文件

### B4. P0-3 最小规则核 / 结构化闭环完成情况
- 已完成什么
- 未完成什么
- 当前成熟度如何
- 证据来自哪些 GitHub 文件

### B5. P0 完成度总结
要求：
- 你必须给出明确的 P0 完成度判断
- 可以量化，也可以分项量化
- 但必须是你自己基于 GitHub 仓库得出的结论
- 不允许偷懒引用分析2来代替判断

## C. 已完成项、未完成项、可复用骨架盘点
要求：
把 GitHub 当前状态拆成三类：

1. **已完成且不应重复建设**
2. **已存在但未做实，需继续收尾**
3. **尚未落地，但已进入本次任务书范围**

这部分的目的是防止 Codex 重复发明已经存在的内容。

## D. 本次新任务书的范围与非目标
要求：
明确列出：
- **本次要做什么**
- **本次不做什么**

这次的范围必须是：

**P0 未完成项 + P1 必要项**

至少明确排除：
- 不做无边界的全量平台化扩张
- 不做全行业 / 全场景规则覆盖
- 不做 case-specific patch
- 不做通过更大模型掩盖 contract 问题
- 不做与当前 GitHub repo 脱节的“理想系统重写”

## E. 目标状态（To-Be for next phase）
要求：
描述本轮任务完成后，系统达到什么状态。
必须回答：
- 哪些 P0 尾项会被收口？
- 哪些 P1 必要项会被补上？
- 哪些 contract 会更稳？
- 哪些 artifacts / matrices / UI / eval 闭环会更完整？
- 哪些文档类型 / pack / review 结果将进入更稳定支持状态？

## F. 下一阶段目标架构与流水线
要求：
必须围绕这条链路展开：

**文档解析层 → 事实抽取层 → 规则命中层 → 证据归档层 → LLM 解释层 → 报告组装层**

同时说明与 L0/L1/L2/L3 的映射关系：

- L0：可视域检查
- L1：硬证据与强约束规则
- L2：条文适用与规范差距
- L3：工程推理与整改编排

要求：
- 说明哪些部分已经有骨架
- 哪些是 P0 尾项
- 哪些是本轮要并入的 P1 必要项
- 说明哪些判断由确定性逻辑完成
- 哪些判断可以留给 LLM 做解释层
- LLM 不能承担第一性审查裁判角色

## G. 核心对象与契约收敛方案
要求：
必须盘点并收敛当前 repo 中与本轮改造强相关的对象，说明：
- 现状
- 问题
- 下一步如何收敛

至少覆盖：
- `DocumentParseResult`
- `AttachmentVisibility`
- `ExtractedFacts`
- `RuleHit`
- `IssueCandidate`
- `FinalIssue`
- `EvidenceSpan`
- `ReviewLayer`
- `FindingType`
- `manualReviewNeeded` 及相关语义字段
- artifact contract / artifactIndex
- UI 消费 contract

如果当前 repo 中存在：
- 字段重复
- 命名并存
- richer internal object 但 final result 丢字段
- visibility / evidence / manual review 状态语义不一致  
你必须明确指出，并给出收敛方案。

## H. 文件级改造清单（必须非常具体）
要求：
这是整份任务书最关键的部分。必须做一个表格，至少包含：

- 优先级（P0-尾项 / P1-必要项）
- 模块
- 当前文件
- 新增文件
- 修改目的
- 具体改动点
- 依赖关系
- 风险
- 验收标准

重点检查并给出改造建议的文件至少包括：
- `apps/api/src/services/document_loader.py`
- `apps/api/src/orchestrator/planner.py`
- `apps/api/src/orchestrator/router.py`
- `apps/api/src/orchestrator/deepresearch_runtime.py`
- `apps/api/src/review/pipeline.py`
- `apps/api/src/review/schema.py`
- `apps/api/src/review/parser/*`
- `apps/api/src/review/extractors/*`
- `apps/api/src/review/rules/*`
- `apps/api/src/review/evidence/*`
- `apps/api/src/review/report/*`
- `apps/api/src/review/evaluation/*`
- `apps/api/src/domain/models.py`
- `apps/api/src/routes/tasks.py`
- `apps/api/src/services/sqlite_store.py`
- `apps/web/src/types/control-plane.ts`
- `apps/web/src/components/home-dashboard.tsx`
- `apps/web/src/components/structured-review-form.tsx`
- `apps/web/src/components/task-detail.tsx`
- `docs/testing.md`
- `fixtures/review_eval/*`
- `tests/test_structured_review.py`
- `tests/test_runtime.py`
- `tests/test_task_service_and_routes.py`
- 其他你认为必须纳入本轮的真实文件

注意：
- 如果某文件当前已经足够好，不要硬写修改
- 如果某文件只需要轻量调整，也要明确说“为什么只需要轻量调整”
- 不要无理由发明大量 repo 中不存在的新文件
- 但你必须在每个文件级建议里明确说明：  
  **该建议将由 Codex 在本地路径 `/Users/lucas/repos/review/008-review-control-plane` 中执行**

## I. PR / Commit 级执行拆分
要求：
把本轮任务拆成可执行的 PR 顺序。建议拆成 5~9 个 PR，每个 PR 说明：

- 改哪些文件
- 为什么先做这个
- 对其他 PR 的依赖
- 回滚边界
- 验收方式

拆分建议应体现：
- 先收敛 P0 contract / boundary / schema 尾项
- 再做 parser / visibility 尾项
- 再做 facts / rules / evidence
- 再做 report / artifact / UI
- 最后做 tests / eval / docs
- 再并入最必要的 P1

但你必须基于 GitHub 仓库真实状态调整顺序，不能机械套模板。

## J. 测试、评测与回归方案
要求：
必须覆盖：
- P0 未完成项的收尾测试
- P1 必要项引入后的最小新增测试
- 单元测试
- 集成测试
- 端到端测试
- 最小 golden / versioned / seed cases 接入
- regression baseline
- 必要的 ablation / cross-pack / cross-model 接口保留

必须明确：
- 当前 GitHub 仓库现有命令中，哪些继续沿用，哪些要补强：
  - `make test`
  - `make test-review-unit`
  - `make test-review-integration`
  - `make eval-review`
  - `make eval-review-ablations`
  - `make eval-review-cross-pack`
  - `make eval-review-cross-model`
  - `make smoke`
  - `make verify-connectivity`
- 本轮至少需要新增或做实哪些断言：
  - visibility 语义
  - facts 抽取
  - rule hits
  - issue 结果
  - severity / layer
  - manual review flag
  - artifact index
  - UI contract
- 至少说明哪些评测指标本轮必须更完整：
  - 问题召回率
  - 重大问题命中率
  - 危大识别命中率
  - 硬证据准确率
  - 附件可视域正确率
  - 严重度校准准确性
  - manual review flag 准确性

## K. Definition of Done（本轮）
要求：
必须给出明确、可验收的完成标准。至少包括：

- P0 未完成项被明确收口
- `structured_review` 在当前 GitHub 仓库的最小支持对象上更稳定运行
- `review_assist` 不回归损坏
- parser / visibility / facts / rules / evidence / report 契约更完整
- `FinalIssue[]` / matrices / artifacts 契约稳定
- 前端能正确展示 structured_review 的核心结果
- tests / eval / docs 同步更新
- 不依赖“把 prompt 写更长”来通过验收
- 任务书可直接交给在本地目录 `/Users/lucas/repos/review/008-review-control-plane` 具有修改权限的 Codex 执行

## L. 风险、边界与开放问题
要求：
列出：
1. 本轮必须解决的
2. 本轮暂不解决、但必须显式挂起的
3. 再往后阶段才处理的

你必须防止把“本次不做”的内容偷偷混入本轮。

## M. 禁止事项 / 反模式
要求：
必须单列一节，至少包括：
- 不得把已存在的 structured_review 骨架误写成“从零开始”
- 不得为了单一冷轧厂样本做正则 / 关键词 patch
- 不得把 `review_assist` 包装成 formal review
- 不得让 LLM 重新成为 formal review 的第一性裁判
- 不得通过“更长 prompt / 更大模型”掩盖 parser、facts、rules、evidence 的 contract 问题
- 不得把“未解析附件 / 系统不可视”直接输出成“文档缺失”
- 不得在 UI 层写业务判定规则去补后端 contract 漏洞
- 不得把未来平台化能力无边界塞进本轮任务

---

# 输出质量要求

请严格遵守：

## 1）不要把任务书写成“完整产品蓝图”
你这次的目标不是重写一份所有阶段的总规划，而是：
- 先检查 P0
- 再输出 “P0 未完成项 + P1 必要项” 的合并任务书

## 2）不要重复发明已存在的东西
如果 GitHub 仓库已存在：
- `structured_review`
- review 子域
- schema
- pipeline
- artifacts
- 前端入口
- tests  
那么重点必须是“收口 / 收敛 / 贯通 / 补齐 / 验收”。

## 3）不要写空话
禁止大量使用：
- 建议优化
- 建议增强
- 建议完善
- 可进一步研究

每条建议都必须落到：
- 哪个文件
- 哪个模块
- 改什么
- 为什么改
- 如何验收

## 4）必须绑定 GitHub 仓库的真实文件与实现
不要写一个脱离 GitHub 仓库的理想系统。  
必须基于当前真实存在的文件、目录、contract、tests、fixtures、docs。

## 5）必须控制新增复杂度
即使引入 P1 必要项，也必须解释：
- 为什么现在必须做
- 为什么这是 P0 未完成后的自然延伸
- 为什么它不会把任务书膨胀成无边界平台化规划

## 6）必须防止过拟合
规则和改造项必须是可复用的，不能做单样本打补丁。

## 7）必须明确“事实源”和“执行路径”不同
你必须在任务书中明确区分：
- **事实源**：GitHub 仓库 + `V0.1-分析2.md`
- **执行路径**：Codex 在本地目录 `/Users/lucas/repos/review/008-review-control-plane` 中落地修改

---

# 输出格式要求

请直接输出一个完整 Markdown 文档，标题固定为：

# 008-review-control-plane P0 完成度检查 + 未完成项/P1 改造任务书（Codex 执行版）

要求：
- 层级清晰
- 适当使用表格
- 语言专业、明确、克制
- 不写宣传稿
- 不写空泛咨询报告
- 不省略文件级和测试级细节

---

# 最后提醒

你不是在复述已有分析，而是在做两件事：

1. **基于 GitHub 仓库独立检查 P0 完成情况**
2. **把 P0 未完成项与 P1 必要项合并成一份可执行任务书**

最终这份任务书将交给 Codex，Codex 会在本地目录：

`/Users/lucas/repos/review/008-review-control-plane`

中执行改造。

因此：
- 不要把已实现骨架误写成“尚未开始”
- 不要把 `V0.1-分析2.md` 中的判断直接当成代码事实
- 不要用很长的战略描述替代文件级施工清单
- 一切事实判断以 GitHub 仓库为准
- 一切执行说明必须落到本地路径 `/Users/lucas/repos/review/008-review-control-plane`

现在开始。请先基于 GitHub 仓库独立检查 P0 完成情况，再直接输出完整任务书正文。