你现在的角色不是“评论者”或“方案顾问”，而是 **资深技术负责人 + Staff Engineer + 架构师 + 交付经理 + 评测负责人**。  
你的任务不是继续比较方案优劣，也不是重写一篇泛泛的架构分析，而是输出一份 **只覆盖 P0：做实已有骨架** 的、可以直接交给 Codex 执行的工程任务书。

---

# 关键前提（务必遵守）

你**看不到我本地文件系统**，也**不能直接检查我电脑上的工作区**。  
你本次可使用的事实来源只有两类：

1. GitHub 仓库地址：  
   `https://github.com/watsonk1998/008-review-control-plane`

2. 一份已有分析文档：  
   `V0.1-分析2.md`

请严格理解以下边界：

- **GitHub 仓库是第一事实源**
- `V0.1-分析2.md` 是第二事实源
- 你不能假设能读取我本机目录内容
- 你不能把“本地工作区里可能存在的内容”写成已确认事实
- 但你必须在任务书中明确写出：  
  **这份任务书将交给具有本地文件操作权限的 Codex 执行，目标本地工作区路径是：**  
  `/Users/lucas/repos/review/008-review-control-plane`

也就是说：

- 你写任务书时，**事实判断必须基于 GitHub 仓库与 V0.1-分析2.md**
- 你写任务书时，**执行落地路径必须明确写成 Codex 将在本地目录**  
  `/Users/lucas/repos/review/008-review-control-plane`  
  **进行修改**

如果 GitHub 仓库与 `V0.1-分析2.md` 不一致，**一律以 GitHub 仓库为准**。

---

# 输入材料优先级

请严格按以下优先级吸收信息：

1. **第一事实源：GitHub 仓库**
   - `https://github.com/watsonk1998/008-review-control-plane`
2. **第二事实源：V0.1-分析2.md**
3. **第三参考：仓库中的 README / docs / tests / fixtures / evaluation 相关说明**
4. **最低优先级：你自己的泛化经验**

如果出现冲突，必须在任务书中显式区分：
- 哪些是 GitHub 仓库已确认事实
- 哪些是 `V0.1-分析2.md` 中的判断
- 哪些是你基于现状提出的建议改造项

---

# 任务目标

请输出一份完整 Markdown 文档，标题固定为：

# 008-review-control-plane P0 改造任务书（Codex 执行版）

这份任务书的用途是：

- 我会把它交给 Codex
- Codex 拥有本地文件修改权限
- Codex 将在以下本地目录中执行改造：  
  `/Users/lucas/repos/review/008-review-control-plane`
- 因此，这份任务书必须足够具体到：
  - 文件级
  - 模块级
  - schema / contract 级
  - test / eval / artifact 级
  - PR / commit 拆分级

---

# 本次任务书的唯一范围：P0

你必须把这次任务书严格限制在 **P0：做实已有骨架**。

## 不允许做的事
- 不把任务书写成 P0/P1/P2 总体路线图
- 不把重点写成未来平台化、运营化、全行业泛化
- 不把本次任务书写成“完整产品架构设计书”
- 不把大量篇幅花在多模态大平台、复杂人工复核平台、全量规则平台、全量 pack 运营体系上
- 不把“换更大模型、加更长 prompt、模仿 Gemini 文风”当作核心改造手段

## 本次只允许做的三件事
### P0-1：做实双轨边界
- 保留 `review_assist`
- 保留 `structured_review`
- 明确它们在 GitHub 当前代码中的职责、调用路径、数据契约和边界
- 明确哪些逻辑应继续留在 orchestration/runtime 层
- 明确哪些逻辑必须下沉到 review 子域
- 避免职责串扰、概念重叠、contract 漂移

### P0-2：做实输入解析与可视域语义
至少覆盖：
- 章节保留
- 表格保留
- 附件 / 图纸 / 图片 / appendix 的状态表达
- 重复内容清洗
- “未解析 / 不可读 / 仅引用到 / 待人工复核”语义
- 避免把“系统没看见”误判成“文档没有”

### P0-3：做实最小可用规则核与结构化审查闭环
优先覆盖：
- 施组结构完整性
- 危大识别
- 重大隐患初筛
- 附件可视域
- 应急预案针对性
- 工期 / 资源基础冲突

---

# 你必须先完成的 GitHub 仓库盘点动作

在写任务书之前，你必须先基于 GitHub 仓库真实内容做盘点，并在任务书中反映盘点结论。你必须至少检查并判断以下对象是否已经存在、成熟度如何、P0 是否真的需要改：

## 后端与 domain / runtime
- `apps/api/src/domain/models.py`
- `apps/api/src/routes/tasks.py`
- `apps/api/src/services/sqlite_store.py`
- `apps/api/src/services/document_loader.py`
- `apps/api/src/orchestrator/planner.py`
- `apps/api/src/orchestrator/router.py`
- `apps/api/src/orchestrator/deepresearch_runtime.py`

## review 子域
- `apps/api/src/review/schema.py`
- `apps/api/src/review/pipeline.py`
- `apps/api/src/review/parser/*`
- `apps/api/src/review/extractors/*`
- `apps/api/src/review/rules/*`
- `apps/api/src/review/evidence/*`
- `apps/api/src/review/report/*`
- `apps/api/src/review/evaluation/*`

## 前端
- `apps/web/src/types/control-plane.ts`
- `apps/web/src/components/home-dashboard.tsx`
- `apps/web/src/components/structured-review-form.tsx`
- `apps/web/src/components/task-detail.tsx`

## 文档、样本、评测与测试
- `README.md`
- `DELIVERY_REPORT.md`
- `docs/architecture.md`
- `docs/formal-review.md`
- `docs/testing.md`
- `docs/known-limitations.md`
- `fixtures/review_eval/*`
- `tests/test_structured_review.py`
- `tests/test_runtime.py`
- `tests/test_task_service_and_routes.py`
- 其他与 structured_review / review_assist / artifacts / eval 直接相关的真实文件

---

# 你必须先回答的关键问题（然后再出任务书）

在任务书的“现状盘点”部分，你必须先基于 GitHub 仓库明确回答：

1. 当前 GitHub 仓库是否已经存在：
   - `structured_review` task type
   - review 子域目录
   - review schema
   - pipeline
   - parser / extractors / rules / evidence / report
   - artifact contract / artifact API
   - structured_review 前端入口与结果渲染
   - tests / fixtures / eval harness

2. 这些能力分别处于什么成熟度：
   - 已实现
   - 初步可用
   - 有骨架
   - 仅 docs / DTO
   - 尚未落地

3. 当前 P0 最真实的短板是什么：
   - runtime / router / planner 的边界不稳？
   - parser / visibility 语义不统一？
   - facts / rule hits / final issues 契约不贯通？
   - evidence / artifact / UI contract 有折损？
   - eval / golden / regression 未接好？
   - 仍存在 fixture-first 假设？
   - 仍存在字段命名重复 / 并存 / 漂移？

4. 哪些事情其实已经做了，不能再被误写成“P0 新增项”？

5. 哪些东西虽然已经存在，但只是“骨架 / 最小原型”，必须纳入本次 P0 的“做实范围”？

---

# 你必须吸收并转译的核心判断

你必须把以下判断转译成工程任务，而不是只复述：

1. 当前 GitHub 仓库应被视为一个  
   **review control plane + structured_review 最小可用原型骨架**

2. 本次不是“从零做 formal review”，而是  
   **把已有 structured_review 骨架做实、做稳、做闭环**

3. P0 不追求：
   - 全 pack 覆盖
   - 全行业 / 全文档类型覆盖
   - 全流程 reviewer 平台
   - 大规模多模态平台
   - 复杂人工复核工作台
   - 深度 L3 工程推理平台

4. P0 真正追求的是：
   - 双轨边界稳定
   - 输入对象稳定
   - visibility 语义稳定
   - facts / rules / evidence / report 契约贯通
   - 最小规则核可运行
   - artifact 与 tests 可回归
   - 至少一到两类当前主支持对象的 formal review 跑稳

5. P0 完成后，系统至少应达到：
   - `structured_review` 可稳定执行最小正式审查链路
   - `review_assist` 不回归损坏
   - 能区分 `missing / referenced_only / attachment_unparsed / parsed / unknown / manual_review_needed` 等相关语义（按 GitHub 当前真实契约收敛，不要强行发明）
   - 能输出稳定的 `FinalIssue[]` / matrices / artifacts
   - 前端能正确消费 structured_review 的核心结果
   - tests / eval / docs 同步更新

---

# 你必须遵守的写作纪律

## 1）GitHub 仓库不是空白系统
不要把当前系统写成“还没有 structured_review / 还没有 review 子域 / 还没有 formal review 骨架”的空白状态。  
你必须先核对 GitHub 仓库，再写“做实已有骨架”。

## 2）不要重复发明已存在的对象
如果 GitHub 仓库已存在：
- `structured_review`
- review 子域
- review schema
- pipeline
- artifacts
- tests
- frontend contract

那么你必须把重点放在：
- 收敛
- 贯通
- 补齐
- 限定边界
- 测试验收
- 修正 contract 漂移

而不是重新发明概念。

## 3）不要写空话
禁止大量使用下列表达而不落地：
- 建议优化
- 建议增强
- 建议完善
- 可进一步研究
- 后续可以探索

每个建议都必须落到：
- 哪个文件
- 哪个模块
- 改什么
- 为什么改
- 如何验收

## 4）不要脱离 GitHub 仓库写理想系统
不要写一个与当前 GitHub 仓库结构无关的“理想架构”。  
必须绑定到当前真实目录、真实模块、真实 contract、真实 tests。

## 5）必须控制新增复杂度
P0 只允许做必要的新文件 / 新对象 / 新测试。  
如果你建议新增较多文件或目录，必须逐项解释为什么这属于 P0 必需，而不是 P1/P2 扩张。

## 6）必须防止过拟合
任务书必须专门写出 P0 不能做的 case-specific patch，例如：
- 看到“煤气区域”就固定报某条
- 看到“汽车吊”就固定报某条
- 看到某个冷轧厂表达就硬编码判断

你必须强调：
P0 只能做 **可复用的最小规则核**，不能做单样本打补丁。

## 7）必须区分三类内容
在整份任务书中显式区分：
- **已确认事实（GitHub 仓库证据）**
- **合理推断**
- **建议改造项**

不得把推断写成既成事实。

## 8）必须写清楚执行落地路径
你不能假设能读取本地目录，但必须在任务书中明确说明：

- **本任务书基于 GitHub 仓库与 `V0.1-分析2.md` 生成**
- **任务书落地执行者是 Codex**
- **Codex 将在本地路径 `/Users/lucas/repos/review/008-review-control-plane` 中执行改造**

这条必须明确写进任务书的“背景 / 适用范围 / 执行说明”部分。

---

# 你输出的任务书必须包含的章节（不得省略）

## A. P0 执行摘要
要求：
- 300~500 字
- 只总结本次 P0 的目标、边界、结果形态
- 明确说明：本次不是做完整 formal review 平台，而是做实已有骨架
- 明确说明：任务书依据 GitHub 仓库与 `V0.1-分析2.md` 生成，执行将在本地路径 `/Users/lucas/repos/review/008-review-control-plane` 由 Codex 落地

## B. 当前 GitHub 仓库真实现状（只写与 P0 相关）
要求：
- 必须基于 GitHub 仓库当前代码与文档
- 重点梳理以下对象的现状：
  - `TaskType` / request / result contract
  - `review_assist`
  - `structured_review`
  - `document_loader` / parser / visibility
  - facts extraction
  - rules / packs
  - evidence / report / matrices / artifacts
  - runtime / router / planner
  - frontend structured_review 入口与结果页
  - tests / fixtures / evaluation
- 对每一项给出成熟度判断：
  - 已实现 / 初步可用 / 有骨架 / docs only / 尚未落地
- 严禁把已实现项错写成“尚未开始”

## C. 本次 P0 的范围与非目标
要求：
明确列出：
- **本次要做什么**
- **本次不做什么**

至少明确排除：
- 不做 P1/P2 扩展
- 不做完整 reviewer workflow 平台化
- 不做全行业 / 全场景规则覆盖
- 不做大规模多模态平台
- 不做 case-specific patch
- 不做“换大模型就当完成”

## D. P0 目标状态（To-Be for P0 only）
要求：
只写 P0 完成后的目标状态，不写最终愿景。
必须回答：
- P0 结束后，哪些链路更稳？
- 哪些 contract 更一致？
- 哪些 artifacts 成为正式输出的一部分？
- 哪些 visibility / manual review 语义会被做实？
- 哪些文档类型 / pack 进入“P0 支持范围”？

## E. P0 级目标架构与流水线
要求：
必须围绕这条链路展开，但只写 P0 需要做实的部分：

**文档解析层 → 事实抽取层 → 规则命中层 → 证据归档层 → LLM 解释层 → 报告组装层**

同时要说明它与 L0/L1/L2/L3 的映射关系：

- L0：可视域检查
- L1：硬证据与强约束规则
- L2：条文适用与规范差距（P0 只做到最小闭环）
- L3：工程推理与整改编排（P0 只保留最小必要解释，不追求深推理平台）

必须说明：
- 哪些判断由确定性逻辑完成
- 哪些判断可以留给 LLM 做解释层
- LLM 不能承担第一性审查裁判角色

## F. P0 需要做实的核心对象与契约
要求：
必须盘点并收敛当前 repo 中与 P0 强相关的对象，说明：
- 现状
- 问题
- P0 如何收敛

至少覆盖：
- `DocumentParseResult`
- `AttachmentVisibility`
- `ExtractedFacts`
- `RuleHit`
- `IssueCandidate`
- `FinalIssue`
- `EvidenceSpan`
- `ReviewLayer`
- `FindingType`
- `manualReviewNeeded` 及相关语义字段

如果当前 repo 中存在：
- 字段重复
- 命名并存
- richer internal object 但 final result 丢字段
- visibility / evidence / manual review 状态语义不一致  
你必须明确指出，并给出 P0 收敛方案。

## G. 文件级 P0 改造清单（必须非常具体）
要求：
这是整份任务书最关键的部分。必须做一个表格，至少包含：

- 优先级（只允许写 P0）
- 模块
- 当前文件
- 新增文件
- 修改目的
- 具体改动点
- 依赖关系
- 风险
- 验收标准

重点检查并给出 P0 改造建议的文件至少包括：
- `apps/api/src/services/document_loader.py`
- `apps/api/src/orchestrator/planner.py`
- `apps/api/src/orchestrator/router.py`
- `apps/api/src/orchestrator/deepresearch_runtime.py`
- `apps/api/src/review/pipeline.py`
- `apps/api/src/review/schema.py`
- `apps/api/src/review/parser/*`
- `apps/api/src/review/extractors/*`
- `apps/api/src/review/rules/*`
- `apps/api/src/review/evidence/*`
- `apps/api/src/review/report/*`
- `apps/api/src/review/evaluation/*`
- `apps/api/src/domain/models.py`
- `apps/api/src/routes/tasks.py`
- `apps/api/src/services/sqlite_store.py`
- `apps/web/src/types/control-plane.ts`
- `apps/web/src/components/home-dashboard.tsx`
- `apps/web/src/components/structured-review-form.tsx`
- `apps/web/src/components/task-detail.tsx`
- `docs/testing.md`
- `fixtures/review_eval/*`
- `tests/test_structured_review.py`
- `tests/test_runtime.py`
- `tests/test_task_service_and_routes.py`
- 其他你认为必须纳入 P0 的真实文件

注意：
- 如果某文件当前已经足够好，不要硬写修改
- 如果某文件只需要轻量调整，也要明确说“为什么只需要轻量调整”
- 不要无理由发明大量 repo 中不存在的新文件
- 但你必须在每个文件级建议里明确说明：  
  **该建议将由 Codex 在本地路径 `/Users/lucas/repos/review/008-review-control-plane` 中执行**

## H. P0 的 PR / Commit 级执行拆分
要求：
把本次 P0 拆成可执行的 PR 顺序。建议拆成 4~7 个 PR，每个 PR 说明：

- 改哪些文件
- 为什么先做这个
- 对其他 PR 的依赖
- 回滚边界
- 验收方式

PR 拆分应优先遵循：
- 先 contract / boundary / schema
- 再 parser / visibility
- 再 facts / rules / evidence
- 再 report / artifact / UI
- 最后 tests / eval / docs 收尾

但你必须基于 GitHub 仓库真实状态调整顺序，不能机械套模板。

## I. P0 测试、评测与回归方案
要求：
本节只写 P0 需要做的测试闭环，不写未来完整评测平台。

必须覆盖：
- 单元测试
- 集成测试
- 端到端测试
- 最小 golden / versioned / seed cases 接入
- regression baseline
- 必要的 ablation / cross-pack / cross-model 接口保留

必须明确：
- 当前 GitHub 仓库现有命令中，哪些继续沿用，哪些要补强：
  - `make test`
  - `make test-review-unit`
  - `make test-review-integration`
  - `make eval-review`
  - `make eval-review-ablations`
  - `make eval-review-cross-pack`
  - `make eval-review-cross-model`
  - `make smoke`
  - `make verify-connectivity`
- P0 至少需要新增或做实哪些断言：
  - visibility 语义
  - facts 抽取
  - rule hits
  - issue 结果
  - severity / layer
  - manual review flag
  - artifact index
- P0 的评测指标至少覆盖：
  - 问题召回率（最小范围）
  - 重大问题命中率（最小范围）
  - 危大识别命中率（最小范围）
  - 硬证据准确率
  - 附件可视域正确率
  - 严重度校准准确性（最小范围）
  - manual review flag 准确性

注意：
P0 不要求你做完整专家黄金库，但必须说明最小可用 golden / seed / regression 应如何接入现有 harness。

## J. P0 Definition of Done
要求：
必须给出明确、可验收的完成标准。至少包括：

- `structured_review` 在当前 GitHub 仓库的最小支持对象上可稳定运行
- `review_assist` 不回归损坏
- parser / visibility / facts / rules / evidence / report 契约贯通
- 能正确区分至少几类附件 / 可视域状态
- `FinalIssue[]` 输出契约稳定
- `artifactIndex` 与 artifact API 可用
- 前端能正确展示 structured_review 的核心结果
- tests / eval / docs 同步更新
- 不依赖“把 prompt 写更长”来通过验收
- 任务书可直接交给在本地目录 `/Users/lucas/repos/review/008-review-control-plane` 具有修改权限的 Codex 执行

## K. 风险、边界与开放问题（仅限 P0）
要求：
列出：
1. P0 内必须解决的
2. P0 内暂不解决、但必须显式挂起的
3. P0 完成后进入 P1 才处理的

你必须防止把“本次不做”的内容偷偷混入 P0。

## L. P0 禁止事项 / 反模式
要求：
必须单列一节，至少包括：
- 不得把已存在的 structured_review 骨架误写成“从零开始”
- 不得为了单一冷轧厂样本做正则 / 关键词 patch
- 不得把 `review_assist` 包装成 formal review
- 不得让 LLM 重新成为 formal review 的第一性裁判
- 不得通过“更长 prompt / 更大模型”掩盖 parser、facts、rules、evidence 的 contract 问题
- 不得把“未解析附件 / 系统不可视”直接输出成“文档缺失”
- 不得在 UI 层写业务判定规则去补后端 contract 漏洞
- 不得把 P1/P2 的平台化能力偷偷塞进 P0

---

# 输出质量要求

请严格遵守：

## 1）不要把任务书写成“完整产品蓝图”
这次只做 P0。  
如果你写成 P0/P1/P2 全量总规划，视为不合格。

## 2）不要重复发明已存在的东西
如果 GitHub 仓库已存在：
- `structured_review`
- review 子域
- schema
- pipeline
- artifacts
- 前端入口
- tests  
那么重点必须是“做实 / 收敛 / 贯通 / 验收”。

## 3）不要写空话
禁止大量使用：
- 建议优化
- 建议增强
- 建议完善
- 可进一步研究

每条建议都必须落到：
- 哪个文件
- 哪个模块
- 改什么
- 为什么改
- 如何验收

## 4）必须绑定 GitHub 仓库的真实文件与实现
不要写一个脱离 GitHub 仓库的理想系统。  
必须基于当前真实存在的文件、目录、contract、tests、fixtures、docs。

## 5）必须控制新增复杂度
P0 只允许做必要的新文件 / 新对象 / 新测试。  
如果你建议新增较多目录或文件，必须逐项说明为什么它们属于 P0 必需。

## 6）必须防止过拟合
P0 只能做 **可复用的最小规则核**，不能做单样本打补丁。

## 7）必须明确“事实源”和“执行路径”不同
你必须在任务书中明确区分：
- **事实源**：GitHub 仓库 + `V0.1-分析2.md`
- **执行路径**：Codex 在本地目录 `/Users/lucas/repos/review/008-review-control-plane` 中落地修改

---

# 输出格式要求

请直接输出一个完整 Markdown 文档，标题固定为：

# 008-review-control-plane P0 改造任务书（Codex 执行版）

要求：
- 层级清晰
- 适当使用表格
- 语言专业、明确、克制
- 不写宣传稿
- 不写空泛咨询报告
- 不省略文件级和测试级细节

---

# 最后提醒

你不是在写未来愿景，而是在写一份 **只做 P0、基于 GitHub 仓库真实现状、可直接交给 Codex 在本地路径 `/Users/lucas/repos/review/008-review-control-plane` 开工的工程任务书**。

因此：
- 不要把已实现骨架误写成“尚未开始”
- 不要把 P1/P2 的未来能力塞进 P0
- 不要用很长的战略描述替代文件级施工清单
- 不要把 `V0.1-分析2.md` 中的推断直接当成代码事实
- 一切事实判断以 GitHub 仓库为准
- 一切执行说明必须落到本地路径 `/Users/lucas/repos/review/008-review-control-plane`

现在开始。请先基于 GitHub 仓库盘点，再直接输出完整任务书正文。