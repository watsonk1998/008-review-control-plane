# 008-review-control-plane《V0.1-分析2》完成度检查 + 未完成项改造任务书（Codex 执行版）

> 标记说明
> **[事实]**：仅指已由 GitHub 仓库当前内容确认的事实。
> **[推断]**：基于代码、文档、测试、UI、评测资产的一致性做出的工程判断。
> **[建议]**：本轮给 Codex 的改造项，不是现状事实。
> 本任务书将 `V0.1-分析2.md` 视为**待核对对象**，不是现状结论。

---

## A. 执行摘要

本任务书的目标不是重写一份新的产品蓝图，而是先以 GitHub 仓库 `watsonk1998/008-review-control-plane` 为第一事实源，独立核对《V0.1-分析2》里关于系统定位、双轨边界、formal review 流水线、parser / visibility / facts / rules / evidence / report / eval / UI / workflow 的判断，确认哪些已经完成、哪些只完成了一半、哪些已经被仓库现状超越，再把**仍未完成或未收口**的部分整理成一份可直接交给 Codex 执行的补完施工清单。仓库当前已经明确把 008 定位为 **review control plane**，同时承载一条独立的 `structured_review` 正式结构化审查子域；`review_assist` 与 `structured_review` 的双轨边界、`parse -> facts -> rules -> evidence -> report` 的 formal review 主链、`sourceDocumentRef` 上传输入、top-level `visibility`、`artifactIndex`、`unresolvedFacts`、reviewer decision、support-scope、versioned eval 与 facts/rule-hit 指标，均已进入 README、docs、后端 schema/runtime、前端类型与页面、以及测试与评测链路。([GitHub][1])

因此，当前仓库绝不能再被描述成“尚未有 structured_review / 尚未有 formal review 子域 / 尚未有 upload / artifact / reviewer workflow / versioned eval”的空白系统；这些内容中相当一部分在 GitHub 现状里已经落地。《V0.1-分析2》关于“它不是空白系统、而是一个已经经历多轮建设后的 structured review control plane”的主判断仍然成立，但其中若干细项已经过时，例如 runtime 不再是 fixture-only、`reviewer-decision` 已有 API/持久化/UI、`visibility` 与 `unresolvedFacts` 已是结果契约的一部分、versioned cases 与 facts/rule-hit/hazard 指标已接入评测主流程、scenario packs 也不再是“全部 placeholder”。 ([GitHub][2])

本任务书因此只聚焦于剩余的收口项：contract 兼容层清理、router/provisional profile 与 review 子域 final profile 的职责收敛、PDF/visibility 的降级语义做实、official-scope packs 与规则深度补强、UI 从“结果浏览页”向“可复核页面”再推进一步、versioned golden / internal-reviewed cases 的扩充与回归加固。最终执行者是**具有本地文件修改权限的 Codex**，它将在本地路径 **`/Users/lucas/repos/review/008-review-control-plane`** 落地本任务书中的修改；事实判断仍以 GitHub 仓库现状为准，若与 `V0.1-分析2.md` 冲突，一律以 GitHub 为准。([GitHub][3])

---

## B. 《V0.1-分析2》完成度检查

### B1. 检查口径说明

本次检查按 8 个主题拆解《V0.1-分析2》：

1. 系统定位与双轨边界
2. formal review 流水线
3. parser / visibility / manual review
4. facts / rules / evidence / report / artifacts
5. pack 体系与规则覆盖
6. canonical contract / API / UI 消费
7. 评测 / versioned cases / regression
8. UI / reviewer decision / workflow

完成状态定义如下：

| 状态   | 判定标准                                         |
| ---- | -------------------------------------------- |
| 已完成  | GitHub 中已有真实代码、真实路由/对象、真实测试或真实 UI 消费，不只是文档概念 |
| 部分完成 | 代码链路已存在，但覆盖范围有限、兼容层未收口、UI/测试/评测尚未完全对齐        |
| 未完成  | 仍停留在文档、DTO、registry、placeholder、空壳入口         |
| 已过时  | 《V0.1-分析2》的说法已被 GitHub HEAD 明显超越，不应再写成新任务    |

本次核对还做了一个路径纠偏：你给出的检查清单里有部分路径已与 GitHub 当前树不一致。仓库中的持久化实现文件是 **`apps/api/src/repositories/sqlite_store.py`**，不是 `apps/api/src/services/sqlite_store.py`；测试文件位于 **`apps/api/tests/`**，不是仓库根目录 `tests/`。以下施工清单全部按 GitHub 当前真实路径书写。([GitHub][4])

### B2. 按主题检查《V0.1-分析2》完成情况

> 下表中“分析2提出的目标/判断”均来自 `V0.1-分析2.md` 的主题主张与收口方向。

| 主题                                            | 分析2提出的目标 / 判断                                                                                                   | GitHub 当前已确认事实                                                                                                                                                                                                                                                                                                                                                                                                                                   | 结论             | 是否进入新任务书                                        |
| --------------------------------------------- | --------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------- | ----------------------------------------------- |
| 系统定位与双轨边界                                     | 008 不是空白系统，而是 control plane；`review_assist` 与 `structured_review` 必须分轨，前者不能冒充 formal review                     | README 将 008 明确定位为 review control plane；`structured_review` 被写成独立 formal review 流水线；首页把 `structured_review` 标为“正式审查”，把 `review_assist` 标为“审查辅助，不给正式审查结论”；runtime 也对两类任务走不同分支，`review_assist` 结果带“不是正式审查结论”的 notice。([GitHub][1])                                                                                                                                                                                                                 | **已完成**        | **否**，只保留边界护栏，不重写定位                             |
| formal review 流水线                             | 应形成 `文档解析 → 事实抽取 → 规则命中 → 证据归档 → LLM 解释 → 报告组装`，且 runtime 只编排不裁判                                                | planner 已显式生成 `parse / extract / rules / evidence / explain / report` 执行阶段，并声明 `structured_review` 属于 `apps/api/src/review`，runtime 只负责 orchestration；architecture 文档也把 Review Pipeline 列为 Parser / Extractors / Pack Registry / Rule Engine / Evidence Builder / Report Builder；pipeline 会落盘 parse/facts/rule-hits/candidates/result/report/matrix 工件。([GitHub][5])                                                                             | **已完成**        | **是**，但属于“边界收口”，不是“新建流水线”                       |
| parser / visibility / manual review           | `DocumentParseResult`、`AttachmentVisibility`、`visibility`、manual review 语义必须真实进入链路；“系统没看见 ≠ 文档没有”               | schema 已有 `VisibilityAssessment`、`DocumentParseResult`、typed `visibility`、`parseWarnings`；docx parser 同时写 `visibility` 和 `visibilityReport`；attachment indexer 已产出 `parsed / attachment_unparsed / referenced_only / missing / unknown`，并区分 `explicit_missing_marker` 与 parser-limited 的 `unknown`；formal-review 文档明确“`manualReviewNeeded` 为 canonical，系统不得把没读到附件写成附件缺失”；测试也验证 PDF 路径为 `pdf_text_only` + `parserLimited=True`。([GitHub][6])      | **部分完成**       | **是**，重点在 PDF 降级语义、UI 消费与契约收敛                   |
| facts / rules / evidence / report / artifacts | `ExtractedFacts / RuleHit / IssueCandidate / FinalIssue / EvidenceSpan` 要贯通，且 issues / matrices / artifacts 要稳定 | schema 与 domain models 已有这些核心对象；`FinalIssue` 继承的 `ReviewIssue` 已保留 `manualReviewNeeded`、`evidenceMissing`、`manualReviewReason`；pipeline 输出 `visibility / resolvedProfile / issues / matrices / artifactIndex / reportMarkdown / unresolvedFacts`；测试验证 construction_org 与 hazardous_special_scheme 的 issue titles、manualReviewReason、evidenceMissing、artifact categories。([GitHub][6])                                                            | **部分完成**       | **是**，重点在 official scope 做深与 UI/contract 统一     |
| pack 体系与规则覆盖                                  | base/scenario packs 不能只停留 registry；需要真实规则覆盖                                                                     | 当前正式支持文档类型是 `construction_org` 与 `hazardous_special_scheme`；base packs 中这两类为 ready，其余三个 doc type 仍是 placeholder；scenario packs 中 `lifting_operations.base`、`temporary_power.base`、`hot_work.base` 已 ready，`gas_area_ops.base`、`special_equipment.base`、`working_at_height.base` 仍 placeholder。([GitHub][7])                                                                                                                                      | **部分完成**       | **是**，但只能做有样本和回归支撑的有界补强                         |
| canonical contract / API / UI 消费              | `visibility`、`manualReviewNeeded`、`artifactIndex`、`unresolvedFacts` 要成为 canonical，前端不应继续依赖 alias / summary text | docs/formal-review 已把 `visibility`、`artifactIndex`、`unresolvedFacts` 定为稳定字段，并声明 `manualReviewNeeded` 为唯一 canonical 布尔语义；TS 类型已消费 `visibility` 与 `unresolvedFacts`；task-detail 也使用 canonical `visibility`、`issues`、`reviewerDecision`。但 `routes/tasks.py` 仍在 issue payload 中注入 `whetherManualReviewNeeded` 兼容 alias，TS 也仍保留这个可选字段。([GitHub][7])                                                                                                   | **部分完成**       | **是**，这是本轮最高优先级之一                               |
| 评测 / versioned cases / regression             | 应有 eval-review / ablations / cross-pack / cross-model，且 versioned cases 与 facts/rule-hit 指标接线                   | `docs/testing.md` 已列出 `make test-review-unit / make test-review-integration / make eval-review / make eval-review-ablations / make eval-review-cross-pack / make eval-review-cross-model / make smoke / make verify-connectivity`；dataset loader 已同时扫描 legacy `metadata.json` 与 versioned `case_metadata.json`；metrics 已统计 `facts_accuracy`、`rule_hit_accuracy`、`hazard_identification_accuracy` 等；harness 已有 `versionedStageGate`。([GitHub][8]) | **已完成 / 部分完成** | **是**，但重点是数据池质量和覆盖面，不是“从零接线”                    |
| UI / reviewer decision / workflow             | structured_review 入口、结果页、reviewer decision API / UI / tests 应形成最小 workflow                                      | 前端首页已支持 fixture 或上传文档的正式审查入口；`StructuredReviewForm` 消费 `supportScope` 并对 skeleton/experimental 做提示；`PUT /api/tasks/{taskId}/reviewer-decision` 路由已存在，domain/models 与 reviewer_decision helper 已支持 task/issue/attachment 级 decision，task-detail 也已挂接 `ReviewDecisionPanel`。但结果页对 attachmentVisibility / conflicts / sectionStructure 仍大量使用 raw JSON 呈现。([GitHub][9])                                                                              | **部分完成**       | **是**，但属于“workflow 补光”而不是“新增 reviewer workflow” |

#### B2.9 关键问题直接回答

1. **《V0.1-分析2》关于系统定位、双轨边界、formal review 架构的核心判断，哪些已经完成？**
   已完成的核心判断包括：008 是 review control plane 而不是单一审查器；`review_assist` 与 `structured_review` 已经分轨；formal review 主链已经真实落到 review 子域；LLM 在 formal review 中被限制为解释层而不是第一性裁判。([GitHub][1])

2. **关于 parser / visibility / facts / rules / evidence / report / artifact 的设计要求，哪些已完成、哪些只完成一半？**
   已完成的部分是核心对象、主链工件、canonical `visibility`、`artifactIndex`、`unresolvedFacts`、official scope 的最小规则核和报告输出；只完成一半的部分是 PDF 降级路径、API alias 清理、UI 对 matrices 的结构化消费、以及 official scope 之外的 pack 覆盖深度。([GitHub][6])

3. **关于评测、golden/versioned cases、回归命令、细粒度指标的要求，哪些已经完成？哪些仍不够？**
   已完成的部分包括：现成回归命令、legacy + versioned 双数据加载、facts/rule-hit/hazard 指标、versioned stage gate、ablation / cross-pack / cross-model 框架；仍不够的是 versioned 数据池广度、truth quality 的层级提升，以及更多 official stage-gate cases。([GitHub][8])

4. **《V0.1-分析2》里哪些内容已经被 GitHub 现状超越，因此不应再作为新任务写给 Codex？**
   不应再写成新任务的内容包括：新增 `sourceDocumentRef` / upload 输入、设计 top-level `visibility`、设计 `unresolvedFacts`、新增 reviewer decision API/UI、建立 support-scope 路由、建立 versioned case loader、建立 facts/rule-hit 指标、把所有 scenario packs 从零创建为 registry。这些都已在 GitHub 现状存在。 ([GitHub][7])

5. **《V0.1-分析2》里哪些方向仍然正确，但现在还没做实，必须继续进入新任务书？**
   仍然正确且必须继续推进的方向是：canonical contract 清理、official-scope packs 与规则深度补强、PDF/visibility 降级语义做实、router 与 review 子域的 profile 收敛、task-detail 从 raw JSON 浏览页升级为更可复核的结果页、以及 versioned/internal-reviewed 数据池扩充。 ([GitHub][10])

6. **如何量化《V0.1-分析2》整体完成度？**
   我的结论是：**按分析2“核心目标的等价实现”计，当前整体完成度约为 77/100。** 这不是“仓库很弱”的意思，而是说明：基础边界、主链、对象、评测框架已经到位，但剩余工作集中在 contract 收敛、official-scope 做深、UI/workflow 补完与数据质量提升。([GitHub][1])

### B3. 完成度量化

**评分口径**：

* 100：代码、测试、UI、文档、评测口径已稳定对齐
* 80：代码与测试基本到位，但范围有限或兼容层仍在
* 60：已有真实代码，但仍偏骨架 / placeholder / 局部打通
* 40：主要停留在 DTO / registry / 文档
* 0：未落地

| 维度                                            | 权重 | 得分 | 理由                                                                                                                            |
| --------------------------------------------- | -: | -: | ----------------------------------------------------------------------------------------------------------------------------- |
| 系统定位与双轨边界                                     | 15 | 14 | README、架构文档、runtime 与首页文案已经一致，`review_assist` 不再冒充 formal review。([GitHub][1])                                                |
| formal review 流水线                             | 15 | 12 | planner、architecture、pipeline、artifacts 已成链，但 provisional profile 与 final profile 仍有收口空间。([GitHub][5])                        |
| parser / visibility / manual review           | 15 | 11 | 对象、状态枚举、manual review 原则都已落地，但 PDF 仍为 text-only 降级，UI/报告对 visibility 的消费还可更稳。([GitHub][6])                                    |
| facts / rules / evidence / report / artifacts | 20 | 15 | 关键对象和 artifacts 全在，official scope 测试也能穿透到 issues/report；不足在覆盖面与 rule depth。([GitHub][6])                                      |
| pack 体系与规则覆盖                                  | 10 |  6 | 已有 ready base packs 与 3 个 ready scenario packs，但其余 doc types 和 scenario packs 仍 placeholder。([GitHub][11])                    |
| canonical contract / API / UI 消费              | 10 |  7 | top-level `visibility`、`artifactIndex`、`unresolvedFacts` 已稳定，但 `whetherManualReviewNeeded` alias 仍残留在路由和 TS 类型里。([GitHub][7]) |
| 评测 / versioned cases / regression             | 10 |  8 | 命令、loader、metrics、stage gate 都在；不足主要是数据池广度和 truth quality，不是基础 wiring。([GitHub][8])                                           |
| UI / reviewer workflow                        |  5 |  4 | 入口、结果页、decision API/持久化/panel 已有；不足是 reviewer 体验仍偏工程调试页。([GitHub][12])                                                        |

**总分：77 / 100**

---

## C. 已完成项、部分完成项、未完成项盘点

### C1. 已完成且不应重复建设

| 事项                                            | 结论                                                        | 证据                                                                                                                                |
| --------------------------------------------- | --------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| `structured_review` 任务类型与请求字段                 | 已完成，不应再写“新增 structured_review”                            | `CreateTaskRequest` 已支持 `fixtureId/sourceDocumentRef/documentType/disciplineTags/strictMode/policyPackIds`。([GitHub][13])         |
| review 子域与正式审查主链                              | 已完成，不应再写“创建 review 子域 / pipeline”                         | README、architecture、planner、pipeline 已明确 formal review 主链和 review 子域职责。([GitHub][1])                                              |
| 上传文档与 `sourceDocumentRef`                     | 已完成，不应再写“设计 upload / sourceDocumentRef”                   | formal-review 文档、upload route 测试、home-dashboard 上传入口均已存在。([GitHub][7])                                                            |
| canonical top-level `visibility`              | 已完成，不应再写“提出 visibility 作为顶层对象”                            | docs/formal-review、schema、pipeline、TS 类型、task-detail 全部已消费 top-level `visibility`。([GitHub][7])                                   |
| `unresolvedFacts`                             | 已完成，不应再写“设计 unresolvedFacts”                              | docs/formal-review、schema、pipeline、TS 类型、task-detail 已打通。([GitHub][7])                                                            |
| reviewer decision 模型 / API / UI / persistence | 已完成，不应再写“新增 reviewer workflow 骨架”                         | models、reviewer_decision helper、`PUT /reviewer-decision`、SQLite、task-detail panel、路由测试均已存在。([GitHub][13])                         |
| support-scope 路由与前端消费                         | 已完成，不应再写“设计 support-scope API”                            | `GET /api/tasks/support-scope` 已存在，表单与结果页都消费它。([GitHub][10])                                                                      |
| versioned cases loader 与 facts/rule-hit 指标    | 已完成，不应再写“把 versioned cases / facts / rule-hit 接到 harness” | dataset 同时加载 legacy + versioned；metrics 已统计 `facts_accuracy`、`rule_hit_accuracy` 等；harness 已有 versioned stage gate。([GitHub][14]) |
| ready scenario packs                          | 已完成部分，不应再写“所有 scenario packs 仅停留在命名层”                     | `lifting_operations.base`、`temporary_power.base`、`hot_work.base` 已是 ready。([GitHub][11])                                          |
| artifact API / artifactIndex                  | 已完成，不应再写“新增 artifact API”                                 | `artifactIndex` 已与 `/api/tasks/{taskId}/artifacts` 共享 catalog。([GitHub][7])                                                       |

### C2. 已部分完成，但仍需收尾

| 事项                                                                 | 当前状态                                                                                       | 收尾方向                                                                         |
| ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------- |
| `manualReviewNeeded` canonical / `whetherManualReviewNeeded` alias | canonical 已确立，但路由与 TS 兼容层仍残留 alias                                                         | 做两阶段退役：先让 UI/API client 只用 canonical，再有计划移除默认 alias 输出。([GitHub][7])         |
| router hint 与 review final profile                                 | planner/router 已明确 “provisional hints”，但 router 仍做 profile 推断，review 子域也做 final resolve    | 保留 router 仅作 hint；final pack/profile 真相只在 review 子域收口。([GitHub][5])          |
| PDF 解析                                                             | 已有显式 `pdf_text_only` 路径和 warnings，但仍属 text-only 降级                                         | 强化 degraded semantics、visibility 解释与回归，不把 unknown 误写成 missing。([GitHub][15]) |
| UI 结果页                                                             | 已能显示核心字段和 reviewer panel，但 attachmentVisibility / conflicts / sectionStructure 仍偏 raw JSON | 做“可复核视图”而不是“重写前端”。([GitHub][16])                                             |
| official scope rule depth                                          | construction_org / hazardous_special_scheme 已可用，但覆盖仍偏窄                                     | 只加有 fixtures / versioned cases 支撑的规则，不做全行业扩张。([GitHub][7])                   |
| versioned 数据池质量                                                    | loader/metrics/harness 已接好，但 stage-gate 官方 cases 仍不多                                       | 扩 versioned / internal-reviewed cases，不降低现有阈值。([GitHub][7])                  |
| reviewer workflow                                                  | API/持久化/UI 已存在，但报告与工件对人工复核状态的呈现还不够顺手                                                       | 补页面展示与测试，不造大平台。([GitHub][17])                                                |

### C3. 未完成，必须进入新的任务书

| 事项                                         | 为什么必须做                                                                                  |
| ------------------------------------------ | --------------------------------------------------------------------------------------- |
| canonical contract 最终收口                    | 当前 executor 结果无 alias，但 route serializer 会重新注入 alias；这是最直接的 contract 漂移点。([GitHub][18]) |
| PDF / visibility 语义回归补强                    | formal review 当前已明确“看不见 ≠ 缺失”，但 PDF 降级路径最容易被误消费；必须补测试与 UI 展示。([GitHub][15])             |
| official scope rules / evidence / facts 做深 | 当前主支持范围仍只有 2 类文档；若不做深，formal review 会停留在 P0 原型。([GitHub][7])                            |
| versioned / internal-reviewed case 扩充      | 评测框架已具备，但数据广度与 truthLevel 仍要加强，否则演进难以稳态。([GitHub][14])                                  |
| 结果页 reviewer cockpit 化                     | API 与 reviewerDecision 已有，但任务详情仍夹杂 raw JSON，不利于稳定复核。([GitHub][16])                      |

### C4. 已过时 / 不再适用，不得再写成 Codex 新任务

| 过时判断                                                      | GitHub 现状                                                                     |
| --------------------------------------------------------- | ----------------------------------------------------------------------------- |
| `structured_review` 仍是 fixture-only                       | 现在是 `fixtureId` / `sourceDocumentRef` 二选一，runtime 也已统一 resolve。 ([GitHub][7]) |
| versioned cases / facts / rule-hit 尚未接入 eval 主流程          | dataset、metrics、harness 已全部接好。 ([GitHub][14])                                 |
| reviewer decision 只是未来设想                                  | 当前已有 domain model、helper、route、store、UI panel、tests。 ([GitHub][13])           |
| `visibility` / `unresolvedFacts` 还只是概念                    | 二者都已在 schema/result/docs/UI 中成为真实字段。 ([GitHub][7])                            |
| 所有 scenario packs 仍为空 placeholder                         | 当前已有 3 个 ready scenario packs。 ([GitHub][11])                                 |
| `FinalIssue` 不保留 `evidenceMissing` / `manualReviewReason` | 当前 domain models、docs、TS 与 UI 都已保留。 ([GitHub][13])                            |

---

## D. 本次新任务书的范围与非目标

### 本次要做什么

本次只做《V0.1-分析2》中**仍未完成或仅部分完成**的部分，而且必须绑定 GitHub 当前真实实现：

1. 收敛 canonical contract：重点是 `manualReviewNeeded` / `whetherManualReviewNeeded`、top-level `visibility` 的唯一消费路径、`unresolvedFacts` 的端到端一致性。([GitHub][7])
2. 收敛 orchestrator 与 review 子域边界：router / planner 只保留 provisional hint，final `resolvedProfile` 与 packs 由 review 子域单点生成。([GitHub][5])
3. 做实 parser / visibility：尤其是 PDF text-only 降级语义、`unknown` / `missing` / `attachment_unparsed` / `referenced_only` 的边界。([GitHub][15])
4. 对 official scope 做深：仅围绕 `construction_org`、`hazardous_special_scheme` 及已 ready 的 scenario packs 补 facts / rules / evidence / report，不做全量 doc type 平台化。([GitHub][7])
5. 补 UI / reviewer workflow 的收尾：在不推翻现有页面的前提下，提升 task-detail 的复核可用性。([GitHub][16])
6. 扩充 versioned/internal-reviewed 回归池，并维持现有 `make eval-review*` 门禁。([GitHub][8])

### 本次不做什么

1. 不做无边界的全量平台化扩张。
2. 不把 `construction_scheme`、`supervision_plan`、`review_support_material` 直接升级成 P0 官方支持对象。当前它们仍是 skeleton / placeholder，应显式保留边界。([GitHub][7])
3. 不做多文档批处理、外部对象存储、多模态 OCR/图纸平台。仓库文档已将这些列为当前限制或扩展方向，而非本轮主线。([GitHub][19])
4. 不做 case-specific patch，不允许看到“煤气区域”“汽车吊”“冷轧厂”就硬编码出固定缺陷。
5. 不用更大模型、更长 prompt 掩盖 parser / facts / rules / evidence 的 contract 问题。formal-review 文档已明确 LLM 不是主裁判。([GitHub][7])
6. 不做“为了更像某个模型文风”的外观工程。

---

## E. 目标状态（To-Be for remaining scope）

本轮完成后，系统应达到的状态是：

1. **分析2剩余项被收口到“收敛”而不是“重建”**：Codex 不再重复建设 `structured_review`、upload、support-scope、reviewer decision、evaluation harness，而是在现有链路上把边界做稳。
2. **contracts 更稳**：API、UI、report、artifacts 只把 `manualReviewNeeded` 当 canonical，`visibility` 只认 top-level 结构化对象，`unresolvedFacts` 在 API / UI / eval 三端含义一致。([GitHub][7])
3. **artifacts / matrices / UI / eval 闭环更完整**：`artifactIndex` 继续作为唯一 catalog；task-detail 不再依赖 raw JSON 才能看懂 visibility / rule hits / conflicts / unresolved facts；versioned stage gate 扩成更可信的数据池。([GitHub][20])
4. **official scope 支持更稳定**：`construction_org`、`hazardous_special_scheme` 与 ready scenario packs 的 facts/rules/evidence 更深、更稳，但 placeholder doc types 仍保持显式边界，不冒充已支持。([GitHub][7])
5. **已知风险被处理或显式挂起**：本轮消除 alias 漂移、PDF visibility 误判与 UI 原始 JSON 依赖；多文档、多模态、strictMode 新语义仍显式挂起，不偷偷混入。([GitHub][19])

---

## F. 与分析2剩余项对应的目标架构与流水线

### F1. 目标流水线

当前 GitHub 已经具备下面这条主链，本轮不是重画，而是收口：

**文档解析层 → 事实抽取层 → 规则命中层 → 证据归档层 → LLM 解释层 → 报告组装层**。planner 与 architecture 文档、以及 review pipeline 的 artifacts 产物都已经证明这条主链存在。([GitHub][5])

| 流水线层    | 当前 GitHub 状态                                                                                                                                 | 本轮要继续补完的点                                                    | 判断主体        |
| ------- | -------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ | ----------- |
| 文档解析层   | 已有 `document_loader.py`、`docx_parser.py`、`pdf_parser.py`、`attachment_indexer.py`；docx 结构化较深，PDF 为显式降级。([GitHub][15])                         | 强化 PDF 降级语义、parseWarnings、source provenance 与 visibility 一致性 | **确定性逻辑**   |
| 事实抽取层   | `project_facts.py`、`hazard_facts.py`、`schedule_resource_facts.py` 已工作，且 `ExtractedFacts` 已含 `factEvidence` 与 `unresolvedFacts`。([GitHub][6]) | 只为 official scope 与 ready packs 补足事实，不为单样本硬编码                | **确定性逻辑**   |
| 规则命中层   | `rules/engine.py` 与 pack registry 已可产生 rule hits，rule hit matrix 也已输出 `packReadiness`。([GitHub][20])                                         | 深化 official scope 的规则，不做全类型一口吃掉                              | **确定性逻辑**   |
| 证据归档层   | `evidence_builder.py` 与 `FinalIssue` 已保留 docEvidence / policyEvidence / evidenceMissing / manualReviewReason。([GitHub][6])                   | 继续保证 issue、report、UI 对这些字段的消费一致                              | **确定性逻辑**   |
| LLM 解释层 | formal-review 文档已将 LLM 明确限制为 title/recommendation/候选整理，而不是 L1/L2/L3 主裁判。([GitHub][7])                                                        | 维持边界，只补 guardrails 和回归，不放权                                   | **LLM 仅解释** |
| 报告组装层   | `report_builder.py`、`matrices.py`、task-detail 已联动。([GitHub][21])                                                                             | 从“能显示”推进到“便于复核”，仍不把业务判定挪到 UI                                 | **结构化组装**   |

### F2. L0 / L1 / L2 / L3 映射

| 层级               | 当前状态                                                                                                                                                                       | 本轮动作                                                     |
| ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- |
| **L0：可视域检查**     | 当前已以 top-level `visibility`、`summary.visibilitySummary`、`matrices.attachmentVisibility`、`manualReviewNeeded` 等形式存在，但 route fallback 与 UI raw JSON 让它的消费仍不够统一。([GitHub][7]) | 继续把 L0 保持为独立顶层对象；不要把 L0 混进 L1 defect judgment；强化 UI 与测试。 |
| **L1：硬证据与强约束规则** | construction_org / hazardous_special_scheme 的最小规则核已落地，tests 已验证关键 issue 命中。([GitHub][20])                                                                                  | 深化 official scope，但不跨越到全行业。                              |
| **L2：条文适用与规范差距** | packs / policyEvidence / clause applicability 已具备基础结构。([GitHub][6])                                                                                                        | 继续做 evidence 与 issue 的显式透传，不把裁判权交给 LLM。                  |
| **L3：工程推理与整改编排** | 现有最小核已包含停机窗口 / 人力 / 高风险工序并行压力等工程推理。([GitHub][20])                                                                                                                          | 只做 bounded deepening，不做开放式大模型推理平台。                       |

---

## G. 核心对象与契约收敛方案

| 对象                                              | GitHub 现状                                                                                                                              | 当前问题                                                     | 本轮收敛方案                                                                                                | 落地文件                                                                                                                                                  |
| ----------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| `DocumentParseResult`                           | 已含 `sections / blocks / tables / attachments / figures / normalizedText / visibility / visibilityReport / parseWarnings`。([GitHub][6]) | 内部对象足够丰富，但 UI 与报告对 parserLimited / parseWarnings 的表达还不均匀 | **保持对象不重构**；只要求 API/UI/report 统一优先消费 `result.visibility` 和 `parseWarnings`，不再从 summary text 反推        | `apps/api/src/services/document_loader.py`、`apps/api/src/review/parser/*`、`apps/api/src/review/pipeline.py`、`apps/web/src/components/task-detail.tsx` |
| `AttachmentVisibility` / `VisibilityAssessment` | 已是 typed enum + top-level canonical result。([GitHub][6])                                                                               | route 还会做 legacy fallback；UI 的 attachment matrix 仍偏 JSON | 保留 top-level `visibility` 为唯一 canonical 结果，legacy fallback 仅用于旧任务兼容；UI 不再把 visibility 当作 summary 的派生物 | `apps/api/src/routes/tasks.py`、`apps/web/src/types/control-plane.ts`、`apps/web/src/components/task-detail.tsx`                                        |
| `ExtractedFacts`                                | 已含 `factEvidence` 与 `unresolvedFacts`。([GitHub][6])                                                                                    | UI 对未决事实展示已存在，但 facts 级回归仍可扩                             | 不改对象定义，补 official-scope facts 与 versioned cases 覆盖                                                    | `apps/api/src/review/extractors/*`、`apps/api/src/review/evaluation/*`、`fixtures/review_eval/*`                                                        |
| `RuleHit`                                       | 已含 `packReadiness / matchType / layerHint / severityHint / factRefs / evidenceRefs`。([GitHub][6])                                      | ready/placeholder 差异虽已进入 matrix，但 pack 选择与规则覆盖深度还不足      | 继续把 `packReadiness` 作为前后端和 eval 的统一信号；不再新增第二套 pack 成熟度语义                                              | `apps/api/src/review/rules/*`、`apps/web/src/components/task-detail.tsx`                                                                               |
| `IssueCandidate`                                | 已保留 richer 证据语义。([GitHub][6])                                                                                                          | 不是对外 contract，应避免 UI 绕过 `FinalIssue` 读 candidates        | 保持内部对象；只通过 `FinalIssue` 对外暴露被批准的字段                                                                    | `apps/api/src/review/evidence/evidence_builder.py`、`apps/api/src/review/pipeline.py`                                                                  |
| `FinalIssue`                                    | 已包含 `manualReviewNeeded / evidenceMissing / manualReviewReason / docEvidence / policyEvidence`。([GitHub][13])                          | 路由和 TS 仍保留 `whetherManualReviewNeeded` alias             | 以 `FinalIssue` 为唯一业务 contract，alias 退役分两步处理                                                           | `apps/api/src/routes/tasks.py`、`apps/web/src/types/control-plane.ts`、`apps/web/src/components/task-detail.tsx`                                        |
| `ReviewLayer` / `FindingType`                   | 已稳定用于 L1/L2/L3 与 issue 呈现。([GitHub][13])                                                                                               | 容易误把 L0 visibility 也塞进 `ReviewLayer`                     | **不要**把 L0 强行并入 `ReviewLayer`；L0 继续用 top-level `visibility` 表达                                        | 文档与测试收口为主                                                                                                                                             |
| `manualReviewNeeded` 相关语义                       | 文档已定义 canonical，测试也验证 direct executor 结果无 alias；但 routes/TS 兼容层仍留 alias。([GitHub][7])                                                  | contract 漂移                                              | Phase A 让 UI/API client 完全不依赖 alias；Phase B 再移除默认 alias 输出                                            | `apps/api/src/routes/tasks.py`、`apps/web/src/types/control-plane.ts`                                                                                  |
| `artifactIndex`                                 | 已是唯一官方 catalog；architecture 明确 UI 不自行推导额外工件真相。([GitHub][20])                                                                           | 无结构性缺陷；主要是 UI 消费仍偏基础                                     | 不改 contract，只增强展示和 tests                                                                              | `apps/api/src/review/pipeline.py`、`apps/web/src/components/task-detail.tsx`                                                                           |
| `resolvedProfile`                               | 已在 pipeline 中生成，并由 planner 明确声明 router 只给 provisional hint。([GitHub][5])                                                               | router / planner / review 子域之间仍有重复逻辑                     | 把“最终 profile 真相”再向 review 子域单点收敛一步                                                                    | `apps/api/src/orchestrator/router.py`、`apps/api/src/orchestrator/planner.py`、`apps/api/src/review/profile_resolver.py`                                |
| report / matrices / summary                     | 已有稳定字段，但 task-detail 对 attachmentVisibility / conflicts / sectionStructure 仍主要展示 JSON。([GitHub][21])                                   | reviewer 体验不够顺手                                          | 保持结构不改，提升结果页渲染粒度                                                                                      | `apps/api/src/review/report/*`、`apps/web/src/components/task-detail.tsx`                                                                              |
| UI / API contract                               | API 与 TS 已基本对齐，reviewerDecision 和 supportScope 也已进类型系统。([GitHub][22])                                                                  | alias 残留 + 部分 raw JSON render                            | 本轮只做“消费 canonical contract”的补完，不写业务判定进 UI                                                             | `apps/web/src/types/control-plane.ts`、`apps/web/src/components/task-detail.tsx`                                                                       |

---

## H. 文件级改造清单

> **执行路径说明**
> 本节所有改动，均由 **Codex** 在本地目录 **`/Users/lucas/repos/review/008-review-control-plane`** 中执行。
> 路径以 GitHub 当前仓库为准；其中持久化实现使用 **`apps/api/src/repositories/sqlite_store.py`**，测试使用 **`apps/api/tests/*`**。([GitHub][4])

### H0. 经检查，本轮不建议做结构性重构的文件

| 模块                     | 当前文件                                                 | 处理结论                         | 原因                                                                                                                    | 执行位置                                                         |
| ---------------------- | ---------------------------------------------------- | ---------------------------- | --------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| domain                 | `apps/api/src/domain/models.py`                      | **保持稳定，仅随 contract 退役做最小同步** | 已有 `SourceDocumentRef`、`ReviewIssue` canonical 字段、`ReviewerDecision*` 模型，不需要重建。([GitHub][13])                         | Codex @ `/Users/lucas/repos/review/008-review-control-plane` |
| review schema          | `apps/api/src/review/schema.py`                      | **保持稳定，仅补注释/校验测试**           | `VisibilityAssessment`、`DocumentParseResult`、`ExtractedFacts`、`FinalIssue`、`StructuredReviewResult` 已到位。([GitHub][6]) | Codex @ `/Users/lucas/repos/review/008-review-control-plane` |
| reviewer workflow core | `apps/api/src/review/reviewer_decision.py`           | **不重写，只补测试**                 | 已具备 default/merge/strict validation。([GitHub][17])                                                                    | Codex @ `/Users/lucas/repos/review/008-review-control-plane` |
| persistence            | `apps/api/src/repositories/sqlite_store.py`          | **默认不改 schema，只在必要时微调**      | 已持久化 `sourceDocumentRef`、`policyPackIds`、`strictMode`、`reviewerDecision`。([GitHub][4])                                | Codex @ `/Users/lucas/repos/review/008-review-control-plane` |
| 首页                     | `apps/web/src/components/home-dashboard.tsx`         | **仅轻量文案/提示调整**               | 已支持 upload、task split、supportScope、正式审查/审查辅助分轨。([GitHub][23])                                                         | Codex @ `/Users/lucas/repos/review/008-review-control-plane` |
| 审查参数表单                 | `apps/web/src/components/structured-review-form.tsx` | **仅轻量 UX 收口**                | 已消费 supportScope，对 skeleton 做警告，`strictMode` 也已降级为 reserved。([GitHub][12])                                            | Codex @ `/Users/lucas/repos/review/008-review-control-plane` |

### H1. 必做改造清单

| 优先级 | 模块                                 | 当前文件                                                                                                                                                                         | 新增文件                                                                                                                                                                     | 修改目的                                        | 具体改动点                                                                                                                                                                                                                                                     | 对应分析2剩余项                              | 依赖关系                                        | 风险                 | 验收标准                                                                                                             | 执行位置                                                         |
| --- | ---------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- | ------------------------------------------- | ------------------ | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| 高   | API contract                       | `apps/api/src/routes/tasks.py`                                                                                                                                               | 无                                                                                                                                                                        | 收敛 canonical contract                       | 1）把 `whetherManualReviewNeeded` 从默认业务输出路径中退役；2）保留 legacy 兼容只能是显式兼容层，而不是默认真相；3）保留 legacy `visibility` backfill 仅用于老任务读取，不允许新结果继续依赖 fallback；4）补 serializer tests                                                                                           | canonical fields / manual review 语义收口 | TS types、task-detail、tests、docs             | 兼容旧前端或旧任务结果        | `structured_review` 默认 API 返回只以 `manualReviewNeeded`、`visibility`、`unresolvedFacts` 为 canonical；兼容策略有明确测试，不再隐式漂移 | Codex @ `/Users/lucas/repos/review/008-review-control-plane` |
| 高   | Frontend types                     | `apps/web/src/types/control-plane.ts`                                                                                                                                        | 无                                                                                                                                                                        | 让前端业务层只认 canonical 字段                       | 1）把 `whetherManualReviewNeeded` 从主业务类型中移到兼容层或删除；2）保留 `visibility`、`unresolvedFacts`、`reviewerDecision` 为正式类型；3）如需兼容，放到 API normalizer，而不是 UI 业务代码                                                                                                         | canonical fields / UI contract        | routes/tasks.py、task-detail.tsx             | TS breakage        | 编译通过，页面逻辑不再读取 alias；新增类型测试或 lint 规则防止回归                                                                          | Codex @ `/Users/lucas/repos/review/008-review-control-plane` |
| 高   | Orchestrator boundary              | `apps/api/src/orchestrator/router.py`、`apps/api/src/orchestrator/planner.py`、`apps/api/src/review/profile_resolver.py`                                                       | 无                                                                                                                                                                        | 收敛 provisional hint 与 final resolvedProfile | 1）router 继续负责能力链选择，不再承担过多 final profile 逻辑；2）planner 明确 `reviewProfile` 只是 hint；3）profile_resolver 成为 documentType/discipline/packs 最终真相生成点；4）将“requested placeholder packs”与“executable ready packs”清楚写入 debug/artifact                                  | boundary / contract 漂移                | runtime、pipeline、tests                      | 影响 plan/debug 兼容   | 同一任务在 plan、runtime、result 中的 profile 解释不再互相打架；`resolvedProfile` 来源清晰，测试覆盖 fixture 与 upload 两条路径                  | Codex @ `/Users/lucas/repos/review/008-review-control-plane` |
| 中   | Runtime parity                     | `apps/api/src/orchestrator/deepresearch_runtime.py`                                                                                                                          | 无                                                                                                                                                                        | 使 fixture 与 upload 路径完全同口径                  | 1）保留 `_resolve_source_document`；2）在 events/debug 中显式记录 `sourceDocumentRef` 归一化结果；3）补 integration tests，确保 fixture 和 upload 至少在 `visibility/resolvedProfile/issues/unresolvedFacts/artifactIndex` 上同口径                                                     | runtime / sourceDocumentRef 收口        | router/planner/profile_resolver             | 事件 JSON 变更         | `structured_review` 通过 fixture 与 upload 创建时，结果契约一致；tests 覆盖两条路径                                                  | Codex @ `/Users/lucas/repos/review/008-review-control-plane` |
| 高   | Parser / visibility                | `apps/api/src/services/document_loader.py`、`apps/api/src/review/parser/pdf_parser.py`、`apps/api/src/review/parser/attachment_indexer.py`                                     | 无                                                                                                                                                                        | 做实 PDF 降级语义与 visibility 边界                  | 1）不重写 parser 栈；2）统一 `parseWarnings / parserLimited / fileType / reasonCounts` 的输出；3）确保 `unknown` 只对应 parser-limited 或无法可靠判断，`missing` 只在有明确缺失证据时产出；4）补 page/provenance 级 warnings；5）新增/扩展测试覆盖 `unknown`、`missing`、`attachment_unparsed`、`referenced_only` | parser / visibility / “系统没看见≠文档没有”    | tests、task-detail、report                    | 误判上升或测试脆弱          | PDF 路径不会把 `unknown` 写成 `missing`；所有附件状态在 API/UI/report 三端一致；测试覆盖四类状态                                             | Codex @ `/Users/lucas/repos/review/008-review-control-plane` |
| 中   | Extractors                         | `apps/api/src/review/extractors/project_facts.py`、`hazard_facts.py`、`schedule_resource_facts.py`                                                                             | 无                                                                                                                                                                        | 为 official scope 补足事实，强化 `unresolvedFacts`  | 1）只围绕现有 official scope 与 ready scenario packs 补事实；2）把关键缺失项明确进入 `unresolvedFacts`；3）不做单案例关键词 patch；4）为 rule-hit 与 hazard identification 的 versioned eval 提供更稳定输入                                                                                           | facts / unresolved facts              | rules/engine、evaluation fixtures            | 过拟合单样本             | versioned eval 的 `facts_accuracy` 不下降；新增事实项都有回归与样本支撑                                                             | Codex @ `/Users/lucas/repos/review/008-review-control-plane` |
| 中   | Rules / packs                      | `apps/api/src/review/rules/engine.py`、`apps/api/src/review/rules/packs/*`                                                                                                    | 如确有样本支撑，可新增极少量 pack 文件；否则无                                                                                                                                               | 对 official scope 与 ready scenario packs 做深  | 1）优先补 `construction_org.base`、`hazardous_special_scheme.base` 与 `lifting_operations.base` / `temporary_power.base` / `hot_work.base` 的 reusable rules；2）placeholder packs 不自动升 official；3）任何新增 rule 都必须对应 fixtures/versioned cases；4）禁止把单一冷轧厂表达写死进规则      | rule kernel / pack 覆盖                 | extractors、evaluation、docs/formal-review.md | 误报/漏报波动            | `make eval-review` 不回退；新增规则有对应 case 和 assertions；无 case-specific hardcode                                        | Codex @ `/Users/lucas/repos/review/008-review-control-plane` |
| 中   | Evidence / report                  | `apps/api/src/review/evidence/evidence_builder.py`、`apps/api/src/review/pipeline.py`、`apps/api/src/review/report/report_builder.py`、`apps/api/src/review/report/matrices.py` | 无                                                                                                                                                                        | 保证 issue / report / artifact 的证据与人工复核语义一致   | 1）确保 `manualReviewReason`、`evidenceMissing`、`docEvidence`、`policyEvidence` 在 result、report、UI 中一致；2）report 中把 `unresolvedFacts` 与 visibility 原因写得更结构化；3）matrix 继续输出 `packReadiness`，不丢成熟度信息                                                               | evidence / report / artifact 消费       | routes/tasks、task-detail、tests              | 报告格式变更影响测试         | 同一 issue 在 JSON、report、UI 中的 manual review 与 evidence gap 含义一致；artifact categories 不变                            | Codex @ `/Users/lucas/repos/review/008-review-control-plane` |
| 高   | UI / reviewer page                 | `apps/web/src/components/task-detail.tsx`                                                                                                                                    | 允许拆出极少量 presentational 子组件；非必须                                                                                                                                           | 把结果页从“工程 JSON 浏览页”提升为“可复核页面”                | 1）保留 `ReviewDecisionPanel`；2）把 `attachmentVisibility`、`ruleHits`、`conflicts`、`sectionStructure` 从 raw JSON 优先渲染为结构化表格/列表；3）raw JSON 仅保留在 `<details>`；4）把 `manualReviewReason`、`evidenceMissing`、`unresolvedFacts`、`visibility.reasonCounts` 放到显眼区域        | UI / reviewer workflow                | TS types、routes/tasks、pipeline/report       | 页面体积增加             | reviewer 在不展开 raw JSON 的情况下即可完成主要复核；build/lint 通过                                                                | Codex @ `/Users/lucas/repos/review/008-review-control-plane` |
| 中   | Evaluation                         | `apps/api/src/review/evaluation/dataset.py`、`metrics.py`、`harness.py`                                                                                                        | 无                                                                                                                                                                        | 不重写 harness，只扩 versioned / stage-gate 能力    | 1）维持 legacy + versioned 双加载；2）不降低现有 thresholds；3）把更多 case-level diagnostics 输出到 report；4）cross-pack / cross-model 结果继续保留                                                                                                                                  | eval / regression 收口                  | fixtures、tests、docs/testing.md              | 评测输出更复杂            | `make eval-review*` 继续可跑；结果能清楚区分 stable cases / versioned stage-gate / diagnostics                               | Codex @ `/Users/lucas/repos/review/008-review-control-plane` |
| 高   | Fixtures / golden / versioned data | `fixtures/review_eval/*`                                                                                                                                                     | 新增若干 `v0.2.x-internal-reviewed` 或等价版本目录及其 `case_metadata.json / expected_facts.json / expected_rule_hits.json / ground_truth_visibility.json / ground_truth_issues.json` | 扩 versioned/internal-reviewed 样本池           | 1）新增至少 4 个 versioned cases，覆盖 construction_org / hazardous_special_scheme / PDF degraded / visibility gap / ready scenario；2）truthLevel、labelStatus 写清楚；3）未 internal-reviewed 的不得直接升 official stage-gate                                                  | versioned/golden 剩余工作                 | evaluation、rules、extractors、docs            | 标注成本、truth quality | 新 cases 被 loader 正常发现；未 reviewed case 不进入 official stage-gate；评测报告可区分 truthLevel                                 | Codex @ `/Users/lucas/repos/review/008-review-control-plane` |
| 中   | Docs                               | `docs/formal-review.md`、`docs/testing.md`、`docs/architecture.md`、`docs/known-limitations.md`                                                                                 | 无                                                                                                                                                                        | 同步代码与文档口径                                   | 1）更新 alias 退役策略；2）更新 official/experimental/skeleton 范围；3）明确 reviewer decision 是已存在 workflow，不是未来愿景；4）保留 `strictMode reserved/no-op`；5）强调不做多文档/多模态平台化                                                                                                      | docs / runbook / testing 命令           | 所有代码 PR                                     | 文档滞后               | 文档与代码、测试、UI 一致；不再出现“已实现能力仍被写成待设计”                                                                                | Codex @ `/Users/lucas/repos/review/008-review-control-plane` |
| 高   | Tests                              | `apps/api/tests/test_structured_review.py`、`apps/api/tests/test_runtime.py`、`apps/api/tests/test_task_service_and_routes.py`                                                 | 如必须可增少量新测试文件；优先不增                                                                                                                                                        | 建立本轮收口的回归护栏                                 | 1）补 fixture/upload parity；2）补 alias/compat 行为；3）补 reviewer decision strict validation；4）补 PDF `unknown/missing`；5）补 `visibility` / `unresolvedFacts` / `artifactIndex` / `packReadiness` / `manualReviewReason` 断言；6）确保 `review_assist` 不回归               | tests / regression 收口                 | 上述所有 PR                                     | snapshot 脆弱、回归成本   | 新增断言全部通过；`review_assist` 回归失败数仍为 0；测试目录使用 GitHub 当前真实路径 `apps/api/tests/*`                                       | Codex @ `/Users/lucas/repos/review/008-review-control-plane` |

---

## I. PR / Commit 级执行拆分

| PR   | 目标                                      | 主要文件                                                                                                                                                           | 为什么先做                                          | 依赖          | 回滚边界                                  | 验收方式                                                                  | 对应分析2剩余项                               |
| ---- | --------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------- | ----------- | ------------------------------------- | --------------------------------------------------------------------- | -------------------------------------- |
| PR-1 | canonical contract 收口                   | `apps/api/src/routes/tasks.py`、`apps/web/src/types/control-plane.ts`、`apps/api/tests/test_task_service_and_routes.py`、`docs/formal-review.md`                  | 先止住 contract 漂移，避免后续 parser/UI/eval 在双语义上继续堆逻辑 | 无           | 仅回滚 serializer/type/docs              | API contract tests + TS build                                         | canonical fields / manual review alias |
| PR-2 | orchestrator 与 review profile 边界收口      | `apps/api/src/orchestrator/router.py`、`planner.py`、`apps/api/src/review/profile_resolver.py`、`apps/api/tests/test_runtime.py`                                  | 在补规则前先统一“谁决定 final profile”                    | PR-1        | 仅回滚 profile/debug 相关逻辑                | runtime tests + plan/result diff                                      | boundary / resolvedProfile             |
| PR-3 | source document parity                  | `apps/api/src/orchestrator/deepresearch_runtime.py`、`apps/api/tests/test_runtime.py`、`apps/api/tests/test_task_service_and_routes.py`                          | fixture 与 upload 必须先同口径，否则后面 eval/UI 都会受影响     | PR-2        | 仅回滚 runtime normalization/event debug | fixture + upload integration                                          | sourceDocumentRef / runtime parity     |
| PR-4 | parser / L0 visibility 做实               | `apps/api/src/services/document_loader.py`、`apps/api/src/review/parser/pdf_parser.py`、`attachment_indexer.py`、`apps/api/tests/test_structured_review.py`       | L0 不稳会污染后续 rules/report/UI                     | PR-3        | 仅回滚 parser/visibility 相关更改            | parser unit tests + official fixtures                                 | visibility / PDF degraded semantics    |
| PR-5 | evidence / report / UI 结果页收口            | `apps/api/src/review/pipeline.py`、`apps/api/src/review/report/*`、`apps/web/src/components/task-detail.tsx`、`apps/web/src/types/control-plane.ts`               | 在 parser 稳后再整理 result/report/UI 对齐             | PR-4        | 可独立回滚 UI 或 report                     | unit/integration + web build                                          | report/artifact/UI consumption         |
| PR-6 | official scope facts / rules / packs 做深 | `apps/api/src/review/extractors/*`、`apps/api/src/review/rules/*`、`apps/api/src/review/evidence/evidence_builder.py`、`apps/api/tests/test_structured_review.py` | 合同与 L0 稳定后，才能安全补深规则                            | PR-5        | 仅回滚规则/事实补强                            | `make test-review-unit` + `make eval-review`                          | official-scope rule depth              |
| PR-7 | versioned/internal-reviewed 回归池扩充       | `fixtures/review_eval/*`、`apps/api/src/review/evaluation/*`、`docs/testing.md`                                                                                  | 在规则补强后更新评测数据与门禁，避免先加 data 后被规则拖累               | PR-6        | 仅回滚新 cases / eval thresholds          | `make eval-review` + `make eval-review-ablations` + diagnostics check | eval / versioned cases                 |
| PR-8 | 文档与运行手册同步收尾                             | `docs/formal-review.md`、`docs/architecture.md`、`docs/known-limitations.md`、必要时 `README.md`                                                                     | 最后统一口径，防止文档继续滞后                                | PR-1 ~ PR-7 | 仅回滚 docs                              | 文档与代码对读                                                               | docs / non-goals / support scope       |

---

## J. 测试、评测与回归方案

### J1. 继续沿用的现有命令

当前仓库已经给出了本轮应继续沿用的命令，不需要另起一套脚本体系：`make test`、`make test-review-unit`、`make test-review-integration`、`make eval-review`、`make eval-review-ablations`、`make eval-review-cross-pack`、`make eval-review-cross-model`、`make smoke`、`make verify-connectivity`。`docs/testing.md` 还明确了这些命令各自覆盖的链路与门禁语义。([GitHub][8])

### J2. 本轮必须新增或做实的断言

| 断言层                      | 必加 / 必强化断言                                                                                                                                                                                           | 关联文件                                                                                                            |
| ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| API contract             | `structured_review` 继续支持 `fixtureId` 与 `sourceDocumentRef`；public API 继续拒绝 `disable_visibility_check`；默认结果不再依赖 `whetherManualReviewNeeded` 作为业务字段；top-level `visibility` 与 `unresolvedFacts` 必须存在且稳定 | `apps/api/tests/test_task_service_and_routes.py`([GitHub][9])                                                   |
| Runtime parity           | fixture 与 upload 两条路径返回的 `visibility / resolvedProfile / issues / artifactIndex / unresolvedFacts` 结构一致                                                                                              | `apps/api/tests/test_runtime.py`、`apps/api/tests/test_task_service_and_routes.py`                               |
| Parser / visibility      | `docx_structured` 与 `pdf_text_only` 两路径继续保留；PDF 场景 `parserLimited=True`；`missing` 仅在 explicit marker 下产出；`unknown` 对应 parser-limited 无法可靠判断                                                          | `apps/api/tests/test_structured_review.py`([GitHub][18])                                                        |
| Facts / unresolved facts | `unresolvedFacts` 继续出现在 result、UI 与 eval 中；新增 facts 需有 versioned 期望值                                                                                                                                 | `apps/api/tests/test_structured_review.py`、`fixtures/review_eval/*`([GitHub][18])                               |
| Rule hits / issues       | `packReadiness` 继续存在于 rule hit matrix；official scope 关键 issue 不回归；`manualReviewReason`、`evidenceMissing` 继续穿透到最终 issue                                                                               | `apps/api/tests/test_structured_review.py`([GitHub][18])                                                        |
| Reviewer decision        | 未知 issueId / attachmentId 必须继续返回 400；合法 reviewer decision 必须被持久化并回读                                                                                                                                  | `apps/api/tests/test_task_service_and_routes.py`、必要时 reviewer_decision 单测([GitHub][9])                          |
| UI contract              | TS 类型只认 canonical 字段；task-detail 不依赖 raw JSON 进行主展示；`npm run lint && npm run build` 必过                                                                                                               | `apps/web/src/types/control-plane.ts`、`apps/web/src/components/task-detail.tsx`、`docs/testing.md`([GitHub][24]) |
| review_assist 回归         | formal review 的收口不得损坏 `review_assist` 非正式链路                                                                                                                                                          | `make test`、`make smoke`、`docs/testing.md`([GitHub][8])                                                         |

### J3. 评测指标与门禁

当前仓库已经把以下指标纳入主评测或 stage gate：`issue_recall`、`l1_hit_rate`、`high_severity_issue_recall`、`pack_selection_accuracy`、`policy_ref_accuracy`、`attachment_visibility_accuracy`、`severity_accuracy`、`manual_review_flag_accuracy`、`hard_evidence_accuracy`、`facts_accuracy`、`rule_hit_accuracy`、`hazard_identification_accuracy`。本轮不允许因为新增工作而下调这些阈值；相反，应维持现有官方门禁，并通过新增 versioned/internal-reviewed 样本提升数据覆盖。([GitHub][20])

### J4. versioned / seed / golden / legacy cases 的本轮要求

1. 保留现有 legacy stable subset，继续作为快速主门禁。([GitHub][8])
2. 扩充 versioned cases，但**未 internal-reviewed 的不得直接升 official stage-gate**。([GitHub][14])
3. 本轮至少新增 4 个 versioned cases，覆盖：

   * construction_org + visibility gap
   * hazardous_special_scheme + ready scenario pack
   * PDF degraded + `unknown` visibility
   * 至少一个 official-scope 之外但仍保持 skeleton/experimental 的观察样本
4. 所有新 case 必须带 `case_metadata.json`、`expected_facts.json`、`expected_rule_hits.json`、`ground_truth_visibility.json`、`ground_truth_issues.json`。([GitHub][14])

---

## K. Definition of Done（本轮）

本轮完成标准如下：

1. 《V0.1-分析2》仍有效的剩余项已经被**明确收口**，且没有要求 Codex 重复建设已存在能力。
2. `structured_review` 在当前主支持对象 `construction_org` / `hazardous_special_scheme` 上的 contract、report、UI、eval 更稳定。([GitHub][7])
3. `review_assist` 不回归损坏，仍保持“辅助审查，不等于正式审查结论”的边界。([GitHub][23])
4. `manualReviewNeeded`、`visibility`、`unresolvedFacts`、`artifactIndex` 的前后端消费口径一致。([GitHub][7])
5. `FinalIssue[]` 保持 `evidenceMissing`、`manualReviewReason`、`docEvidence`、`policyEvidence`，且 route / UI 不再偷偷退回 alias 字段。([GitHub][13])
6. task-detail 能以结构化方式展示 visibility、rule hits、conflicts、section structure、reviewer decision，而不是主要依赖 raw JSON。
7. `make test-review-unit`、`make test-review-integration`、`make eval-review`、`make eval-review-ablations`、`make eval-review-cross-pack`、`make eval-review-cross-model`、`make smoke`、`make verify-connectivity` 在本轮要求的场景下通过。([GitHub][8])
8. docs 与代码同步更新，且不依赖“把 prompt 写更长”来通过验收。
9. 任务书可被具有本地文件修改权限的 Codex 直接在 **`/Users/lucas/repos/review/008-review-control-plane`** 执行。

---

## L. 风险、边界与开放问题

### L1. 本轮必须解决

1. API / TS / UI 对 `manualReviewNeeded` 的 alias 漂移。
2. PDF `unknown` / `missing` / `attachment_unparsed` / `referenced_only` 的误消费风险。
3. router / planner / review 子域在 `resolvedProfile` 上的重复判断。
4. task-detail 对 raw JSON 的依赖过高。
5. official scope rule depth 不足带来的“看起来能审、实际上只到骨架”的风险。

### L2. 本轮暂不解决，但必须显式挂起

1. 多文档批处理。
2. 外部对象存储。
3. 多模态 OCR / 图纸理解平台。
4. `strictMode` 的新裁决语义。
5. 把 skeleton doc types 升级为 official。
   这些都已被仓库文档明确标为当前限制或后续扩展，不得偷偷混入本轮。([GitHub][19])

### L3. 再往后阶段才处理

1. 更广的行业/专业 pack 运营。
2. reviewer feedback 回流与大规模工作台化。
3. YAML/DSL 规则 authoring 平台。architecture 文档已明确 evidence packs 继续使用 Python/Pydantic registry 管理，本轮不转平台。([GitHub][20])
4. 多模型 explanation benchmarking 的长期治理，而不是本轮即时扩张。

---

## M. 禁止事项 / 反模式

1. 不得把已存在的 `structured_review` 骨架误写成“从零开始”。
2. 不得为了单一冷轧厂样本、煤气区域样本、汽车吊样本写正则 / 关键词 patch。
3. 不得把 `review_assist` 包装成 formal review。仓库已明确它只是辅助审查。([GitHub][23])
4. 不得让 LLM 重新成为 formal review 的第一性裁判。formal-review 文档已明确 LLM 只做解释层。([GitHub][7])
5. 不得通过“更长 prompt / 更大模型”掩盖 parser、facts、rules、evidence 的 contract 问题。
6. 不得把“未解析附件 / 系统不可视”直接输出成“文档缺失”。formal-review 文档与 attachment indexer 已明确禁止这类误判。([GitHub][7])
7. 不得在 UI 层写业务判定规则去补后端 contract 漏洞。UI 只能消费 canonical contract，不能发明新真相。([GitHub][20])
8. 不得把未来平台化能力无边界塞进本轮。
9. 不得把 placeholder pack、skeleton doc type、reserved `strictMode` 伪装成已成熟能力。([GitHub][7])

---

## N. 给 Codex 的执行说明

1. **事实来源顺序**
   先看 GitHub 仓库当前 HEAD，再把 `V0.1-分析2.md` 当作待核对对象。若冲突，GitHub 优先。 ([GitHub][3])

2. **执行路径**
   你将在本地目录 **`/Users/lucas/repos/review/008-review-control-plane`** 中执行改造；本任务书只提供基于 GitHub 事实的施工顺序，不假设我能看到你本地工作区。

3. **路径解释**
   本任务书里的文件路径以 GitHub 真实路径为准：

   * 持久化文件：`apps/api/src/repositories/sqlite_store.py`
   * 测试目录：`apps/api/tests/*`
     不按旧路径别名施工。([GitHub][4])

4. **优先顺序**
   先做 contract / boundary / canonical fields；再做 parser / visibility；再做 report / UI；再做 official-scope facts/rules/evidence；最后做 eval/data/docs。不要机械套模板，按上文 PR 顺序推进。

5. **每个 PR 前后必须跑的最小验证**

   * contract / route / runtime PR：`make test-review-unit`、`make test-review-integration`
   * parser / rules / eval PR：额外执行 `make eval-review`
   * UI PR：`cd apps/web && npm run lint && npm run build`
   * 最终收口：`make test`、`make eval-review-ablations`、`make eval-review-cross-pack`、`make eval-review-cross-model`、`make smoke`、`make verify-connectivity`。([GitHub][8])

6. **提交策略**
   采用小步 PR / commit，保证每个 PR 的回滚边界清晰；不要在一个 PR 里同时做 contract 改造、UI 重绘、pack 扩张和数据集迁移。

7. **实现原则**
   只补未完成项，不重写已完成项；只做 reusable rule，不做 case patch；只让 LLM 解释，不让 LLM 裁判；只做 bounded expansion，不搞无边界平台化。

---

这份任务书的核心结论只有一句：**当前 GitHub 仓库已经完成了大量《V0.1-分析2》中的核心建设，Codex 的工作不是再发明这些能力，而是把尚未收口的 contract、visibility、official-scope depth、UI/workflow 与 eval/data 做实。**

[1]: https://github.com/watsonk1998/008-review-control-plane "https://github.com/watsonk1998/008-review-control-plane"
[2]: https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/src/orchestrator/deepresearch_runtime.py "https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/src/orchestrator/deepresearch_runtime.py"
[3]: https://github.com/watsonk1998/008-review-control-plane/blob/main/DELIVERY_REPORT.md "https://github.com/watsonk1998/008-review-control-plane/blob/main/DELIVERY_REPORT.md"
[4]: https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/src/repositories/sqlite_store.py "https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/src/repositories/sqlite_store.py"
[5]: https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/src/orchestrator/planner.py "https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/src/orchestrator/planner.py"
[6]: https://raw.githubusercontent.com/watsonk1998/008-review-control-plane/main/apps/api/src/review/schema.py "https://raw.githubusercontent.com/watsonk1998/008-review-control-plane/main/apps/api/src/review/schema.py"
[7]: https://github.com/watsonk1998/008-review-control-plane/blob/main/docs/formal-review.md "https://github.com/watsonk1998/008-review-control-plane/blob/main/docs/formal-review.md"
[8]: https://github.com/watsonk1998/008-review-control-plane/blob/main/docs/testing.md "https://github.com/watsonk1998/008-review-control-plane/blob/main/docs/testing.md"
[9]: https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/tests/test_task_service_and_routes.py "https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/tests/test_task_service_and_routes.py"
[10]: https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/src/routes/tasks.py "https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/src/routes/tasks.py"
[11]: https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/src/review/rules/packs/__init__.py "https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/src/review/rules/packs/__init__.py"
[12]: https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/web/src/components/structured-review-form.tsx "https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/web/src/components/structured-review-form.tsx"
[13]: https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/src/domain/models.py "https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/src/domain/models.py"
[14]: https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/src/review/evaluation/dataset.py "https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/src/review/evaluation/dataset.py"
[15]: https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/src/review/parser/pdf_parser.py "https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/src/review/parser/pdf_parser.py"
[16]: https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/web/src/components/task-detail.tsx "https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/web/src/components/task-detail.tsx"
[17]: https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/src/review/reviewer_decision.py "https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/src/review/reviewer_decision.py"
[18]: https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/tests/test_structured_review.py "https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/tests/test_structured_review.py"
[19]: https://github.com/watsonk1998/008-review-control-plane/blob/main/docs/known-limitations.md "https://github.com/watsonk1998/008-review-control-plane/blob/main/docs/known-limitations.md"
[20]: https://github.com/watsonk1998/008-review-control-plane/blob/main/docs/architecture.md "https://github.com/watsonk1998/008-review-control-plane/blob/main/docs/architecture.md"
[21]: https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/src/review/pipeline.py "https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/api/src/review/pipeline.py"
[22]: https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/web/src/types/control-plane.ts?plain=1 "https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/web/src/types/control-plane.ts?plain=1"
[23]: https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/web/src/components/home-dashboard.tsx "https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/web/src/components/home-dashboard.tsx"
[24]: https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/web/src/types/control-plane.ts "https://github.com/watsonk1998/008-review-control-plane/blob/main/apps/web/src/types/control-plane.ts"
