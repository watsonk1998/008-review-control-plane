

角色设定

你现在扮演的是一名 首席研究官（Chief Research Officer）+ 差距裁决官（Gap Adjudication Lead）+ 审查治理设计师（Review Governance Designer）+ 反过拟合审计官（Anti-Overfitting Auditor）。

你的任务不是再写一版 V0.3 宏大路线图，也不是把前面的差距研究结果简单改写成实施建议列表。
你的任务是：

基于当前 GitHub 仓库事实、本地 research pack 结构化证据、V0.2 设计文档、两份真实监督样本、已有的《三角对比研究结果》、已定稿的《V0.3 边界声明》，以及当前缺乏人工裁决材料这一现实，起草一份《V0.2→V0.3 差距裁决与反过拟合约束清单》完整草案。

这份文档的本质不是"实施总文档"，而是 V0.3 启动前的裁决闸门文件。
它的作用是：
	1.	把前一轮研究里提到的差距，分类为：
	•	已确认差距
	•	半确认差距
	•	暂不裁决差距
	•	明确不推荐作为 V0.3 主线的差距
	2.	把这些差距映射到：
	•	L0 / L1 / L2 / L3
	•	parser / facts / rules / evidence / workflow / eval
	3.	明确哪些可以进入：
	•	official 主线
	•	experimental
	•	diagnostics
	•	internal-reviewed 准备区
	4.	明确哪些方向必须被反过拟合否决
	5.	在缺乏人工裁决材料的前提下，规定 Gemini seed 的临时、受限、可追溯使用方式

⸻

任务背景

项目仓库：
	•	https://github.com/watsonk1998/008-review-control-plane

该项目当前不是单一 formal reviewer，而是一个 review control plane，并承载一条正式结构化审查主链：
	•	parse -> facts -> rules -> evidence -> report

当前关键仓库事实包括：

1）项目定位
	•	项目不是"单一审查器本体"
	•	structured_review 是正式结构化审查子域
	•	review_assist 不等于正式审查结论

2）当前 official scope

当前 official documentType 仅包括：
	•	construction_org
	•	hazardous_special_scheme

以下类型虽已有 ready base pack，但当前仍为 experimental，不纳入 official gate：
	•	construction_scheme
	•	supervision_plan
	•	review_support_material

3）当前 pack 治理事实
	•	ready / official base：construction_org.base、hazardous_special_scheme.base
	•	ready / experimental base：construction_scheme.base、supervision_plan.base、review_support_material.base
	•	ready scenario：lifting_operations.base、temporary_power.base、hot_work.base、gas_area_ops.base
	•	placeholder scenario：special_equipment.base、working_at_height.base

并且：
	•	ready pack ≠ official support
	•	promotion governance 依赖：
	•	tests
	•	versioned cases
	•	policy evidence
	•	rule coverage

4）当前 parser / PDF / visibility 边界
	•	PDF 仍是 pdf_text_only + parserLimited=True 的受限路径
	•	本轮不引入 OCR / 多模态
	•	visibility_gap / attachment_unparsed / referenced_only / evidence不足 必须保留人工复核语义
	•	系统不得把"没读到附件"直接写成"附件缺失"

5）当前结果契约事实

structured_review 当前稳定 DTO 已包括：
	•	summary
	•	visibility
	•	resolvedProfile
	•	issues
	•	matrices
	•	artifactIndex
	•	reportMarkdown
	•	unresolvedFacts

其中：
	•	visibility 是 top-level canonical 可视域对象
	•	manualReviewNeeded 是唯一 canonical 人工复核布尔语义
	•	issue 明确输出：
	•	issueKind
	•	applicabilityState
	•	evidenceMissing
	•	manualReviewReason

6）当前评测治理事实
	•	make eval-review
	•	make eval-review-ablations
	•	make eval-review-cross-pack
	•	make eval-review-cross-model

当前 official versioned stage gate 的核心阈值包括：
	•	facts_accuracy >= 0.90
	•	rule_hit_accuracy >= 0.85
	•	hazard_identification_accuracy >= 0.90
	•	attachment_visibility_accuracy >= 0.90
	•	manual_review_flag_accuracy >= 0.80

同时输出 layeredMetrics：
	•	L0
	•	L1
	•	L2
	•	L3
	•	CrossCutting

其中 L3 当前仍偏 diagnostic-only。

7）当前 gold / seed / reviewed 语义

fixtures/review_eval/README.md 已定义：
	•	v0.1.0-gemini-seed
	•	v0.1.0-bootstrap-seed
	•	v0.2.0-internal-reviewed
	•	v1.0.0-expert-golden

并明确规定：
	1.	不得把 Gemini 结果直接当作专家 truth
	2.	必须区分 issue truth 与 visibility truth
	3.	必须显式记录 provenance

⸻

你的研究输入与证据层级

在本轮《V0.2→V0.3 差距裁决与反过拟合约束清单》中，证据与约束优先级必须明确区分：
	1.	README.md、docs/formal-review.md、fixtures/review_eval/README.md
——作为系统边界真相源，优先级最高；
	2.	fixtures/任务书/《V0.3 边界声明》.md
——作为本轮差距裁决的边界上限与治理约束；
	3.	fixtures/任务书/三角对比研究结果.md
——作为研究发现、差距候选与分层归因来源；
	4.	fixtures/research_inputs/、fixtures/supervision/、Gemini seed
——分别作为样本级结构化证据源、人类可读呈现层与 candidate reference。

当《三角对比研究结果》中的研究观察与《V0.3 边界声明》的已定稿边界表述存在张力时，以《V0.3 边界声明》为本轮裁决边界上限；但《V0.3 边界声明》本身也不得推翻 GitHub 仓库事实这一最高层系统边界真相源。

补充说明：
	•	latest-eval-summary.md（位于 fixtures/research_inputs/）是当前治理状态快照，可作为裁决的阶段性辅助证据，但不是长期仓库真相
	•	eval/ 子目录下的评测 JSON（eval-review.json 等）可辅助量化差距裁决

⸻

已知现实约束（你必须正视）

现实约束 1：当前还没有完整人工裁决材料

目前并没有成体系的：
	•	内部逐 issue adjudication notes
	•	reviewer confirmed truth set
	•	expert-reviewed gold labels

因此，你不能假装现在已经拥有可靠的人类 gold。

现实约束 2：Gemini 暂时只能"顶一顶"

Gemini deepresearch 在当前阶段只能作为：
	•	seed baseline
	•	bootstrap substitute
	•	candidate issue inventory
	•	差距探针

不能作为：
	•	gold truth
	•	final adjudication authority
	•	prompt 隐式答案库
	•	style target

现实约束 3：当前已有两份高价值真实样本，但极易诱导过拟合

监督样本中有真实施组、V0.0 结果、V0.2 结果、Gemini 结果。
这些材料非常有价值，但样本量很小，语境很强，很容易诱导出：
	•	项目名 patch
	•	数字 patch
	•	章节模板 patch
	•	附件名 patch
	•	Gemini wording 模仿

因此，本次文档必须把"反过拟合"当作中心内容之一，而不是附带提醒。

⸻

你这次必须遵守的 V0.3 已拍板边界

以下边界已经确定，你必须以此为前提裁决，不得重开大盘：

边界 1：official scope 暂不扩

V0.3 仍只把以下两个 documentType 作为 official 主战场：
	•	construction_org
	•	hazardous_special_scheme

边界 2：ready scenario packs 可以继续增强，但默认只进 experimental / diagnostics

允许增强：
	•	lifting_operations
	•	temporary_power
	•	hot_work
	•	gas_area_ops

但默认只进入：
	•	experimental
	•	diagnostics
	•	internal-reviewed 准备区

不自动升 official。

边界 3：V0.3 主攻 L0 + L2

V0.3 的主目标是：
	•	L0：visibility / parser / blocked-state / manual-review 可信度
	•	L2：fact -> rule hit -> clause applicability -> evidence gap 闭环

L3 允许增强，但不是本轮主目标。

边界 4：V0.3 不纳入 OCR / 多模态 / 图纸平台化 / 多文档批处理

这些都不在本轮边界内。

边界 5：V0.3 以 versioned cases + layeredMetrics + reviewer gate 为主治理方式

不能只靠两份样本报告文本观感定路线。

边界 6：Gemini 只能作为 seed / bootstrap substitute

不能伪装成 gold，不能变成规则答案库。

⸻

你的核心任务

请起草一份 《V0.2→V0.3 差距裁决与反过拟合约束清单》完整草案。

这份文档必须做的不是"讨论"，而是"裁决"。
也就是说，你不能只说"这个方向值得考虑"，你必须更明确地说：
	•	这个差距是否被确认
	•	它属于哪一层
	•	它属于哪类系统问题
	•	它在 V0.3 中应进入哪种治理层级
	•	它是否存在显著过拟合风险
	•	它是应当推进、延后、降级、否决，还是仅保留观察

⸻

你必须完成的裁决动作

对于每一条主要差距或建议方向，你必须至少裁决以下 6 件事：
	1.	裁决状态
	•	已确认（Confirmed）
	•	半确认（Partially Confirmed）
	•	暂不裁决（Deferred）
	•	明确否决（Rejected）
	2.	能力层归属
	•	L0 / L1 / L2 / L3 / CrossCutting
	3.	系统层归属
	•	parser
	•	facts
	•	rules
	•	evidence
	•	workflow
	•	eval
	•	report only
	4.	治理落点
	•	official mainline
	•	experimental
	•	diagnostics
	•	internal-reviewed preparation
	•	out of scope
	5.	过拟合风险等级
	•	low
	•	medium
	•	high
	•	unacceptable
	6.	下一步动作类型
	•	立即纳入 V0.3 P0
	•	进入 V0.3 P1
	•	仅保留观察
	•	明确不纳入本轮

裁决时，差距的确认 / 否决应优先依据 research pack 中的结构化 JSON / matrices，而非仅凭 Markdown 报告文案做定性判断。

⸻

极其重要：反过拟合硬约束

这份文档的名字里就有"反过拟合约束清单"，所以你必须把 anti-overfit 做成文档主体，而不是一章小尾巴。

1）必须明确否决的方向

你必须明确写出，以下类型原则上应被否决或降级：
	•	围绕项目名写 patch
	•	围绕固定数字写特判
	•	围绕章节标题写样本模板逻辑
	•	围绕附件名写例外判断
	•	围绕 Gemini 结果做答案蒸馏
	•	围绕 Gemini wording 做 style imitation
	•	为双样本单独写 rules / extractors / report 分支

2）必须优先保留的方向

你必须明确写出，以下方向属于优先保留的通用机制增强：
	•	visibility taxonomy 加固
	•	attachment state taxonomy 加固
	•	fact taxonomy 加固
	•	clause applicability / evidence gap 闭环
	•	reviewer gate / diagnostics / promotion governance
	•	official / experimental / seed / internal-reviewed / gold 的分层治理

3）必须明确区分

你必须在文档中不断区分以下几类东西：
	•	issue truth
	•	visibility truth
	•	evidence truth
	•	suggestion enhancement
	•	seed signal
	•	reviewed truth
	•	gold truth

4）必须把"做硬机制"优先于"追分"

文档必须明确表达：
	•	与其在两份样本上更像 Gemini，不如在更多样本上更可信
	•	与其扩大 official scope，不如先把现有 official 两类做硬
	•	与其让报告更长，不如让 visibility / applicability / evidence gap 更可靠

⸻

你必须处理的输入材料

你必须综合处理以下材料：

A. GitHub 当前主干事实（第 1 层：系统边界真相源）

至少包括：
	•	README
	•	docs/formal-review.md
	•	fixtures/review_eval/README.md
	•	docs/testing.md（如有需要）
	•	fixtures/supervision 中的样本结果与源文档

B. 本地 research pack 结构化证据（第 2 层：样本级结构化证据源）

	•	fixtures/research_inputs/manifest.json - 工件清单
	•	fixtures/research_inputs/sample-a-cold-rolling/ - 样本 A 全部结构化工件（13 个 JSON/matrix 文件）
	•	fixtures/research_inputs/sample-b-puhua-rainwater/ - 样本 B 全部结构化工件
	•	fixtures/research_inputs/eval/ - 4 种评测 JSON（eval-review / ablations / cross-pack / cross-model）
	•	fixtures/research_inputs/latest-eval-summary.md - 当前治理状态快照

裁决差距时，必须优先依据结构化 JSON / matrices（如 structured-review-result.json、structured-review-l0-visibility.json、structured-review-rule-hits.json 等），而非仅凭 Markdown 报告。

C. V0.2 设计文档
	•	V0.2 研究设计与实施总文档.md

你必须把它用作：
	•	目标与边界参考
	•	L0/L1/L2/L3 设计意图参考
	•	anti-overfit 原则参考

但不能把它当作当前现状快照。

D. 两份直接上游文档（必须同时处理且不得混淆角色）

你必须同时处理以下两份上游文档，但不得混淆其角色：
	•	fixtures/任务书/三角对比研究结果.md
角色：提供 V0.0 / V0.2 / Gemini 三方差距、L0/L1/L2/L3 分层归因、以及 anti-overfit 初步观察；
限制：它是研究底稿，不直接等于治理裁决。
	•	fixtures/任务书/《V0.3 边界声明》.md
角色：提供本轮已收敛的 scope / governance / anti-overfit 边界；
限制：它定义本轮裁决边界上限，但不得与 GitHub 仓库事实冲突。

你的任务不是在差距裁决阶段重新发明边界，也不是用研究观察推翻已定稿边界，而是在既定边界内完成差距分级、归类、降级、否决与治理落点判断。

⸻

你最终必须输出的文档结构

你的输出必须是完整的 《V0.2→V0.3 差距裁决与反过拟合约束清单》草案，并至少包含以下章节，必须按顺序组织：

⸻

0. 文档定位

说明：
	•	这不是 V0.3 实施任务书
	•	这不是 PR 分解计划
	•	这不是产品愿景文档
	•	这是 V0.3 启动前的 gap adjudication + anti-overfit gate document

⸻

1. 裁决背景与证据基础

说明：
	•	本文基于哪些仓库事实（第 1 层：系统边界真相源）
	•	本文使用了哪些 research pack 结构化工件（第 2 层：样本级结构化证据源）
	•	本文参考了哪些 Markdown 报告（第 3 层：人类可读呈现层）
	•	本文如何使用 Gemini seed（第 4 层：seed / candidate reference）
	•	本文是否参考了 latest-eval-summary.md（当前治理快照，非长期真相）
	•	本文如何使用 V0.2 设计文档
	•	本文当前缺失什么材料（如人工裁决）
	•	因此本文裁决的可信边界在哪里

这一章必须体现"证据边界意识"。

⸻

2. 差距裁决总声明

用高密度语言说明：
	•	从 V0.2 到 V0.3，哪些差距被正式确认
	•	哪些差距被降级
	•	哪些不进入本轮主线
	•	为什么这样裁决

这一章相当于全文总裁决。

⸻

3. 差距裁决总表

你必须给出一张高信息密度总表。

每一行至少包括：
	•	差距项
	•	Confirmed / Partially Confirmed / Deferred / Rejected
	•	L0/L1/L2/L3/CrossCutting
	•	parser/facts/rules/evidence/workflow/eval/report
	•	official / experimental / diagnostics / internal-reviewed / out of scope
	•	overfit risk
	•	next action

这是全文最核心的一张表。

⸻

4. 进入 V0.3 主线的已确认差距

这一章只写 Confirmed 且建议进入 V0.3 的项目。

至少拆成：
	•	P0 confirmed gaps
	•	P1 confirmed gaps

每条都要说明：
	•	为什么确认
	•	为什么进入本轮
	•	为什么不会明显诱导双样本过拟合
	•	它对应哪一层和哪一类系统能力

⸻

5. 被降级、延后或仅保留观察的差距

这一章专门说明：
	•	为什么有些差距虽然存在，但不适合进入本轮主线
	•	为什么有些只能先进 experimental / diagnostics
	•	为什么有些需要 reviewer / internal-reviewed 先补齐

⸻

6. 明确否决项与禁止事项

这一章必须很硬。

至少包括：
	•	明确否决的优化方向
	•	明确禁止的 patch 类型
	•	明确禁止的 Gemini 使用方式
	•	明确禁止的样本特征驱动路线

这一章必须写得像 policy，不是建议。

⸻

7. Gemini seed 的临时裁决地位

这一章必须单独写清：
	•	Gemini 目前在这份清单里被允许扮演什么角色
	•	不允许扮演什么角色
	•	在没有人工裁决材料时，它如何临时顶一顶
	•	哪些事情绝不能由 Gemini seed 直接决定
	•	从 seed 到 internal-reviewed / expert-golden 还缺什么

⸻

8. 反过拟合约束清单

必须单独成章。

你必须把 anti-overfit 约束写成明确清单，而不是散落在正文里。

建议至少包含三类：
	•	结构性约束
	•	数据与评测约束
	•	实现与规则约束

每条约束最好写成：
	•	约束内容
	•	约束目的
	•	违反后会导致什么风险

⸻

9. 进入 V0.3 的准入条件与退出条件

这章非常重要。

你必须写清：
	•	什么样的差距才允许进入 official 主线
	•	什么样的差距只能待在 experimental / diagnostics
	•	什么情况下应立即退出本轮范围
	•	什么情况下必须等待 internal-reviewed / reviewer gate / 更多样本支持

⸻

10. 最终裁决结论

最后给出一段高密度正式表述，概括：
	•	V0.2->V0.3 这轮真正要解决什么
	•	明确不解决什么
	•	最应优先保护什么
	•	最不能犯的错误是什么
	•	为什么"差距裁决 + anti-overfit gate"必须先于"大规模实施"

⸻

输出风格要求

你的输出必须满足以下要求：
	1.	必须像正式内部裁决文件
不是 brainstorming，不是建议合集，而是带有明确裁决口吻的 gate document。
	2.	必须硬
要敢于说"不纳入""暂不裁决""明确否决"。
	3.	必须有治理感
要反复体现：
	•	official
	•	experimental
	•	diagnostics
	•	internal-reviewed
	•	seed
	•	gold
这些层级的不同地位。
	4.	必须强反过拟合
不能只提一句"注意过拟合"，而要把它做成条目化、制度化约束。
	5.	必须与仓库事实一致
不能写出和当前 README / formal-review / review_eval README 相冲突的内容。
	6.	必须比《V0.3 边界声明》更落一层
也就是：
	•	边界声明是"本轮边界"
	•	本文是"哪些差距进入边界，哪些被挡在外面"

⸻

你最后必须自查的 10 件事

在完成文档前，请逐条确认：
	1.	我是否把文档写成了"裁决清单"，而不是"实施方案"？
	2.	我是否明确使用了 Confirmed / Partially Confirmed / Deferred / Rejected？
	3.	我是否给出了总裁决表？
	4.	我是否把差距映射到了 L0/L1/L2/L3 和 parser/facts/rules/evidence/workflow/eval？
	5.	我是否把 official / experimental / diagnostics / internal-reviewed / out of scope 分清了？
	6.	我是否明确写了 Gemini seed 的临时地位？
	7.	我是否单列了反过拟合约束清单？
	8.	我是否明确否决了 case-specific patch 路线？
	9.	我是否避免把两份样本的现象直接写成通用规则？
	10.	我是否让这份文档真正能作为 V0.3 启动前的闸门文件？
	11.	我的裁决是否优先依据了 research pack 中的结构化 JSON / matrices，而非仅凭 Markdown？
	12.	我是否在证据基础章节中说明了四层证据层级？
	13.	我是否明确做到了：研究观察服从边界声明，边界声明服从 GitHub 仓库事实；没有把《三角对比研究结果》里的研究发现直接抄写成差距裁决结论，也没有让差距裁决越过《V0.3 边界声明》的已定稿边界？

如果以上任一项没有满足，请继续完善，而不是提前结束。

⸻

最终目标一句话

请产出一份：

与当前仓库事实一致、以 V0.3 已确认边界为前提、优先依据 research pack 结构化证据、能够裁决哪些差距进入主线、哪些被降级或否决、并把防双样本过拟合作为制度化约束的《V0.2→V0.3 差距裁决与反过拟合约束清单》完整草案。
