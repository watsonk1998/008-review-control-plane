

你现在不是普通程序员，而是这个仓库的 首席落地工程师 / 收口负责人。
你的任务不是“提建议”，不是“再写一版设计稿”，也不是“做局部 demo”，而是基于既有底本与当前代码，把 008-review-control-plane 的 V0.2 方案按 PR1 ~ PR8 完整落地到可验收状态。

0. 角色与执行原则

你必须同时扮演以下三个角色：
	1.	实施工程师：亲自修改代码、测试、文档、评测资产。
	2.	验收工程师：每做完一段都要以 DoD 和测试为准，不接受“理论上应该行”。
	3.	收口架构师：确保 contract、runtime、review domain、eval、UI、docs 只有一个真相源，不制造第二真相。

你必须遵守以下硬约束：
	•	这次允许大范围修改，不以“文件数少”为目标。
	•	但绝不允许无边界乱改；每个改动都必须能归属到 PR1~PR8 的明确任务。
	•	不得只做“看起来更像 Gemini 的文风”。
	•	不得为冷轧厂/培花等具体样本写 case-specific patch。
	•	不得把 visibility gap 写成文档缺失。
	•	不得把 ready pack 自动等同于 official support scope。
	•	不得把 review_assist 伪装成正式审查结论。
	•	不得引入新的第二真相字段，尤其不能让 legacy alias 重新变成 canonical。
	•	不得跳过测试、评测、文档同步。
	•	最终交付必须是能运行、能测试、能评测、能解释、能回归的一套完整工程结果。

⸻

1. 第一事实源与工作目标

第一事实源

你的第一事实源是以下两类内容：
	1.	仓库当前代码现实
	2.	《V0.2 研究设计与实施总文档.md》

若历史认知、旧分析、旧 prompt 与当前仓库冲突，一律以当前仓库与 V0.2 任务书为准。

总目标

你要把当前仓库推进到：

一个真正完成 PR1-PR8 的 V0.2 工程版本：
structured_review 在 fixture/upload 双路径下 contract 等价，
L0/L1/L2/L3 结果可解释、可分层评测、可人工复核、可持久化回放，
official / experimental / skeleton 边界清楚，
reviewer workflow 可闭环，
docs / tests / eval / UI / API 全部对齐。

不是“做到大概”，不是“做到主要功能”，而是要把 PR1~PR8 全部做完。

⸻

2. 你要完成的真实交付，不是建议

你要直接输出并落地以下成果：
	1.	完整代码修改
	2.	完整测试修改
	3.	完整评测资产修改
	4.	完整文档更新
	5.	必要迁移/兼容处理
	6.	最终验收清单
	7.	每个 PR 阶段的完成说明
	8.	未解决风险清单（仅允许极少量真实未决项，不得把主任务留空）

⸻

3. PR1-PR8 的执行要求（必须全部完成）

⸻

PR1 — Contract spine 收口

目标
统一 domain / review schema / routes / sqlite / TS types 的 contract spine，形成唯一真相源。

必做事项
你必须完成以下事项：
	•	收口 apps/api/src/domain/models.py
	•	收口 apps/api/src/review/schema.py
	•	收口 apps/api/src/routes/tasks.py
	•	收口 apps/api/src/repositories/sqlite_store.py
	•	收口 apps/web/src/types/control-plane.ts

必须达到的状态
	•	manualReviewNeeded 是 canonical
	•	visibility 是 canonical
	•	reviewerDecision* 相关结构是 canonical
	•	whetherManualReviewNeeded 只能作为 legacy replay alias，不得再扩散
	•	不得新增第二真相字段
	•	create/get/list/result/stream 的类型语义一致
	•	SQLite 回放对旧任务、坏 JSON、缺字段要稳健，不得轻易炸
	•	source_document_ref_json、reviewer_decision_json round-trip 必须稳定
	•	TS types 与后端 schema 必须零漂移

重点要求
	•	优先使用类型系统消除漂移
	•	优先消除 contract 重复表达
	•	优先补稳兼容与 round-trip 安全

⸻

PR2 — Source ingress / loader 收口

目标
让 fixture 与 upload 两条 structured_review 路径 contract 等价。

必做事项
你必须完成以下文件/模块的收口：
	•	apps/api/src/routes/uploads.py
	•	apps/api/src/services/document_loader.py
	•	apps/api/src/orchestrator/deepresearch_runtime.py
	•	apps/web/src/components/home-dashboard.tsx

必须达到的状态
	•	支持稳定单文件 upload → SourceDocumentRef
	•	fixtureId 与 sourceDocumentRef 在 structured_review 路径上等价
	•	documentType / disciplineTags / policyPackIds / sourceDocumentRef 传递链完整
	•	不得出现 fixture 和 upload 两条链结果语义不一致
	•	MIME/provenance/fileType 错误信息必须稳定、可解释
	•	不允许静默吞掉上传文件差异
	•	runtime 必须显式处理 source resolver，不得靠隐式分支凑合

⸻

PR3 — L0 visibility / parser / manual-review artifact 完整化

目标
把 L0 做成真正独立的一层，而不是夹在结果里的模糊附属品。

必做事项
你必须完成以下方面：
	•	apps/api/src/review/parser/*
	•	apps/api/src/review/pipeline.py
	•	apps/web/src/components/task-detail.tsx
	•	与 L0 对应的 tests

必须达到的状态
	•	DOCX / PDF / MD / TXT 都返回稳定的：
	•	parseMode
	•	visibility
	•	parseWarnings
	•	parserLimited
	•	manualReviewNeeded
	•	manualReviewReason（如适用）
	•	单独产出 L0 visibility artifact
	•	attachment_unparsed / referenced_only / missing / unknown / parsed 等状态必须清楚
	•	必须严格坚持：
	•	未解析 ≠ 缺失
	•	text-only 可视域 ≠ 正文完整可见
	•	PDF 仍应保持保守表达，不允许虚假自信
	•	UI 必须明确展示 visibility / parser limitations / unresolved facts / manual review reasons

重点要求
	•	不要靠文案掩盖 parser 能力不足
	•	要让 L0 成为可评测、可审计、可复核的一层

⸻

PR4 — Official scope 的 L1/L2 规则与证据核加固

目标
优先把 official 文档类型做硬，而不是盲目扩面。

必做事项
你必须深化：
	•	apps/api/src/review/extractors/*
	•	apps/api/src/review/rules/engine.py
	•	apps/api/src/review/evidence/*
	•	apps/api/src/review/report/*

必须达到的状态
	•	official doc types 的 L1 规则更稳
	•	L2 的 policy refs / clause applicability / evidence gap 表达更完整
	•	unresolvedFacts 不再只是挂件，而是贯通到结果、报告、评测
	•	必须显式区分：
	•	hard defect
	•	visibility gap
	•	evidence gap
	•	suggestion enhancement
	•	issue 中如有必要，增加最小可行字段，例如：
	•	applicabilityState
	•	issueKind
	•	report 不得把 inference 写成 hard evidence
	•	report 不得把 visibility 缺口写成正文缺失

重点要求
	•	优先加固 construction_org
	•	优先加固 hazardous_special_scheme
	•	其余 doc type 不得为了“看起来做很多”而粗暴扩张

⸻

PR5 — support-scope / promotion / profile resolution 收口

目标
统一 official / experimental / skeleton / placeholder 的唯一真相源。

必做事项
你必须完成：
	•	apps/api/src/review/support_scope.py
	•	apps/api/src/review/profile_resolver.py
	•	apps/api/src/review/rules/packs/*
	•	apps/web/src/components/structured-review-form.tsx
	•	docs/formal-review.md

必须达到的状态
	•	resolvedProfile 的最终裁决只在一个地方发生
	•	planner/router 只能提供 provisional hints，不得再次成为最终真相
	•	support-scope 成为唯一范围真相源
	•	ready pack 不得误导为 official support
	•	form/UI 必须真实呈现 official / experimental / skeleton / placeholder
	•	pack promotion 要有硬条件：
	•	tests
	•	versioned cases
	•	policy evidence
	•	rule coverage
	•	所有文档与 API/UI 表述一致

⸻

PR6 — Layered eval / stage gate 制度化

目标
把评测从“能跑”变成“能管住工程”。

必做事项
你必须完成：
	•	apps/api/src/review/evaluation/*
	•	fixtures/review_eval/*
	•	docs/testing.md
	•	apps/api/tests/test_review_evaluation.py

必须达到的状态
	•	legacy baseline 继续可跑，且防回归
	•	official versioned cases 成为主 stage gate
	•	experimental versioned cases 只进 diagnostics
	•	eval 输出必须按 L0/L1/L2/L3 组织
	•	指标至少包括并正确落地：
	•	attachment_visibility_accuracy
	•	manual_review_flag_accuracy
	•	hard_evidence_accuracy
	•	high_severity_issue_recall
	•	facts_accuracy
	•	rule_hit_accuracy
	•	policy_ref_accuracy
	•	必要的 L3 diagnostics
	•	make eval-review
	•	make eval-review-ablations
	•	make eval-review-cross-pack
	•	make eval-review-cross-model
都必须真正可用、输出可解释

重点要求
	•	不能只改 harness 文案
	•	不能只改 thresholds 常量
	•	必须让测试、fixtures、metrics、docs、命令输出形成闭环

⸻

PR7 — Reviewer cockpit 与写回闭环

目标
让人工复核真正闭环，而不是只有一个“能写 reviewerDecision JSON”的半成品。

必做事项
你必须完成：
	•	apps/api/src/review/reviewer_decision.py
	•	apps/api/src/services/task_service.py
	•	apps/api/src/routes/tasks.py
	•	apps/web/src/components/task-detail.tsx

必须达到的状态
	•	reviewer decision 支持 issue-level / attachment-level 写回
	•	只允许更新当前结果中存在的 issue/attachment id
	•	状态回写后能持久化
	•	重读 task detail 时能正确显示
	•	UI 可完成最小复核闭环，不依赖 raw JSON
	•	必须清楚区分：
	•	confirmed
	•	rejected
	•	needs supplement
	•	unresolved / pending
	•	attachment / issue bucket 要与 report/UI 分类一致
	•	不允许未知 id 静默成功

⸻

PR8 — 文档统一归拢与发布前回归

目标
让“代码真相”和“文档真相”不再分裂。

必做事项
你必须完成：
	•	docs/architecture.md
	•	docs/formal-review.md
	•	docs/testing.md
	•	必要的 README
	•	其他相关 docs 若被代码变化影响，也要同步收口

必须达到的状态
	•	planner/router 只被描述为 provisional
	•	review 子域被描述为最终 formal review 决策层
	•	official / experimental / skeleton 范围与代码一致
	•	reviewer workflow 文档与 API/UI 一致
	•	eval 命令、阈值、输出说明与真实实现一致
	•	所有示例 payload、字段名、行为描述不得过时

⸻

4. 测试要求：不是可选项，是交付主体

你必须把测试当成主交付，不是附属品。

必须重点补齐/更新的测试

至少完成并更新以下测试体系：
	•	apps/api/tests/test_structured_review.py
	•	apps/api/tests/test_runtime.py
	•	apps/api/tests/test_task_service_and_routes.py
	•	apps/api/tests/test_review_evaluation.py

必须覆盖的断言方向

至少覆盖：
	1.	contract spine 一致性
	2.	sqlite round-trip 安全
	3.	legacy replay 兼容
	4.	fixture/upload parity
	5.	L0 visibility artifact
	6.	parser warnings / parserLimited
	7.	unresolvedFacts / applicability / evidence-gap
	8.	official / experimental 边界
	9.	reviewerDecision 写回与重读
	10.	artifactIndex authoritative
	11.	eval 的 L0-L3 grouped metrics
	12.	official gate / experimental diagnostics 的分离

禁止行为
	•	禁止只改 snapshot 不改逻辑
	•	禁止把测试删掉当“修复”
	•	禁止为了过测试硬编码 case-specific 判断
	•	禁止在 rules/extractors/report 中按项目名/fixture id 分支

⸻

5. 评测与样本资产要求

你必须正确使用评测资产，不能偷懒。

必做
	•	扩充/整理 fixtures/review_eval/*
	•	必要时补 internal-reviewed 路径
	•	保持 seed、baseline、official gate、experimental diagnostics 的清晰分层
	•	保留 Gemini 结果仅作 seed/baseline/reference，不得当 gold truth

强约束
	•	不得把 Gemini wording 当目标
	•	不得把 Gemini 输出当最终真值
	•	不得因两份高价值样本而过拟合
	•	不得把 visibility truth 和 issue truth 混写

⸻

6. 你的真实实施顺序

你虽然可以一次改很多文件，但执行逻辑必须严格按这个收口顺序推进：
	1.	先完成 PR1
	2.	再完成 PR2
	3.	再完成 PR3
	4.	再完成 PR4
	5.	再完成 PR5
	6.	再完成 PR6
	7.	再完成 PR7
	8.	最后完成 PR8

即使你在一个工作流里同时改多个文件，输出时也必须按上述阶段明确说明：
	•	哪些文件属于哪一阶段
	•	每一阶段解决了什么问题
	•	该阶段的测试/评测如何通过
	•	哪个 DoD 被满足了

⸻

7. 最终 DoD（全部必须满足）

最终交付必须同时满足以下条件：
	1.	structured_review 的 fixture 与 upload 两条路径 contract 等价
	2.	result.visibility 是唯一 canonical L0 contract
	3.	whetherManualReviewNeeded 仅保留 legacy replay 兼容
	4.	official scope 的 L1/L2 regression 通过
	5.	facts_accuracy、rule_hit_accuracy、attachment_visibility_accuracy、manual_review_flag_accuracy 在 gate 中可见且稳定
	6.	reviewer decision 可写回、持久化、重读、展示
	7.	report/UI 明确分离 hard defect、visibility gap、evidence gap、suggestion enhancement
	8.	fixtures/construction 如需新增 derived assets，必须不污染原始规范文本
	9.	docs 与代码行为完全一致
	10.	至少以下命令能真实通过并有可信输出：

	•	make test
	•	make test-review-unit
	•	make test-review-integration
	•	make eval-review
	•	make eval-review-ablations
	•	make eval-review-cross-pack
	•	make eval-review-cross-model

⸻

8. 输出格式要求（你最后必须这样汇报）

你的最终输出必须按以下结构给出，不要废话，不要泛泛描述：

A. 实施总览
	•	你完成了哪些 PR 阶段
	•	每个阶段对应哪些文件
	•	每个阶段解决了什么核心问题

B. PR1 ~ PR8 逐阶段完成说明

对每个 PR 分别给出：
	•	修改文件清单
	•	修改目标
	•	关键实现点
	•	风险控制点
	•	测试与评测结果

C. 关键 contract 变化
	•	列出 canonical 字段
	•	列出 legacy alias 兼容策略
	•	列出 round-trip / replay / migration 策略

D. 关键 L0/L1/L2/L3 落地说明
	•	L0：visibility / parser / manual review
	•	L1：hard rules
	•	L2：policy refs / evidence / unresolved facts
	•	L3：workflow / remediation bucket / reviewer feedback

E. 测试与评测结果
	•	列出新增/修改的测试
	•	列出 eval 结果结构
	•	说明 official gate 与 experimental diagnostics 的区分

F. 文档同步结果
	•	哪些 docs 更新了
	•	如何保证与代码一致

G. 剩余风险
	•	仅允许列出真正还未解决但不阻断合并的少量问题
	•	不允许把核心任务列为“未来再做”

⸻

9. 你绝不能犯的错误

以下错误一旦出现，视为任务失败：
	1.	只更新 docs，不真正改代码
	2.	只改代码，不补 tests/eval/docs
	3.	只改字段名，不处理 round-trip / replay
	4.	把 visibility 问题写成正文缺失
	5.	把 ready pack 写成 official
	6.	让 planner/router 再次拥有最终 profile 真相
	7.	用 case-specific patch 让样本过关
	8.	用更长 prompt/更大模型掩盖工程问题
	9.	不说明 PR1-PR8 的完成归属
	10.	未达到 DoD 却宣称“已经完成 V0.2”

⸻

10. 执行态度要求

你现在要做的不是“展示理解”，而是交付工程结果。
把自己当成这次 V0.2 收官负责人：
	•	该补的模型补
	•	该补的 route/store 补
	•	该补的 parser/pipeline 补
	•	该补的 tests 补
	•	该补的 fixtures/eval 补
	•	该补的 UI/docs 补

目标只有一个：

让 PR1-PR8 全部达到可验收、可回归、可合并的 100% 完成度。

开始执行。








