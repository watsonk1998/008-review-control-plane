

你现在不是“帮忙改几个文件”的 AI 程序员。
你现在的角色是：
	•	首席实现工程师
	•	首席回归负责人
	•	首席自验收官

你的唯一任务是：
基于仓库当前统一基线 Commit 430c4a7，严格按照《V0.2 研究设计与实施总文档.md》的要求，把 PR1 ~ PR8 对应工作 全部补齐到 100% 完成度和 100% 验收质量。
你不能挑容易的做，不能只做表层文案，不能只做“看起来像完成”的改动。
你必须交付的是：能跑、能测、能回归、能解释、能合并 的完整工程结果。 ￼

⸻

0. 第一原则

你必须把这次工作理解成：

不是做一个漂亮但空心的架构 PR。
而是把当前 structured_review 主链，按 V0.2 任务书，彻底补齐 contract、ingress、L0、L1/L2、support-scope、layered eval、reviewer cockpit、docs 与全量测试闭环。

你的一切执行都必须服从以下硬约束：
	1.	以 GitHub 当前主干与《V0.2 研究设计与实施总文档》为第一事实源。
	2.	以 G2 的 PR1~PR8 任务拆分为唯一执行主线。
	3.	以 G1 的文件级清单为落地边界。
	4.	以 G3/G4 的测试、评测、DoD 为强制验收标准。
	5.	禁止用注释、TODO、文案、空壳类、伪 contract、伪 adapter 冒充完成。
	6.	禁止只改代码不补测试。
	7.	禁止只改文档不改实现。
	8.	禁止引入本机路径、个人 worktree 路径、私有环境假设。
	9.	禁止 case-specific patch。不得按冷轧厂/培花项目名、fixture id、文件名写特判。
	10.	禁止把 Gemini wording 当目标。禁止把 seed 结果当专家 truth。
	11.	禁止把 visibility gap 写成缺失。
	12.	禁止让 legacy alias 重新变成第二真相源。
	13.	禁止把 ready pack 自动等同 official support scope。
	14.	禁止以“后续再补测试/后续再补评测/后续再补 UI”为借口交差。 ￼

⸻

1. 你的交付目标

你必须一次性交付 PR1 ~ PR8 全量完成版，不是 partial，不是 skeleton，不是“先搭个架子”。

最终结果必须满足：
	•	structured_review 的 fixture 与 upload 两条路径 contract 等价
	•	visibility / manualReviewNeeded / reviewerDecision / artifactIndex / support-scope / resolvedProfile / unresolvedFacts 形成单一真相源
	•	official scope 的 L1/L2 rules + evidence + report + eval 闭环可回归
	•	L0/L1/L2/L3 分层评测制度化
	•	reviewer decision 可写回、可持久化、可重读、可展示
	•	docs / API / TS types / tests 完全对齐
	•	所有必需命令 可执行且通过 ￼

⸻

2. 执行方式要求

你必须按以下方式工作：

2.1 先做事实盘点，再动代码

先系统核对以下事实，不允许凭想象开改：
	•	当前仓库目录与文件实际位置
	•	sqlite_store.py 的真实路径
	•	tests 的真实路径
	•	当前已有的 structured_review 主链契约
	•	当前已有的 support-scope / reviewer decision / upload / eval / artifact API 实现
	•	当前 Makefile / test / eval 命令的真实入口

2.2 必须按 PR1 → PR8 顺序推进

不允许胡子眉毛一把抓。
必须先收紧 contract spine，再做 ingress，再做 L0，再做 rules/evidence，再做 support-scope，再做 eval，再做 reviewer cockpit，最后做 docs 归拢。 ￼

2.3 每做一层，必须同步补测试

你不能出现这种垃圾情况：
	•	改了 models/schema，没补 route/store tests
	•	改了 parser/pipeline，没补 structured_review tests
	•	改了 upload/runtime，没补 fixture/upload parity tests
	•	改了 evaluation，没补 review_evaluation tests
	•	改了 UI types，没确认后端 contract 对齐

2.4 绝不允许“文档快于代码”

文档只能反映已经落地的代码事实，不能超前吹牛。

⸻

3. 逐 PR 强制执行任务

⸻

PR1 — Contract spine 收口（必须做到 100%）

目标
把 V0.2 所需的 canonical contract、持久化 round-trip、API serialization、前端 TS types 一次收口。

必改文件
	•	apps/api/src/domain/models.py
	•	apps/api/src/review/schema.py
	•	apps/api/src/routes/tasks.py
	•	apps/api/src/repositories/sqlite_store.py
	•	apps/web/src/types/control-plane.ts

必做内容
	1.	明确 canonical 字段：
	•	visibility
	•	manualReviewNeeded
	•	reviewerDecision
	•	artifactIndex
	•	resolvedProfile
	•	unresolvedFacts
	2.	whetherManualReviewNeeded 仅保留 legacy replay/serialization 兼容，不得重新参与内部判断。
	3.	sqlite_store.py 必须实现安全 round-trip：
	•	source_document_ref_json
	•	reviewer_decision_json
	•	坏 JSON / 老数据 / 不完整 payload 不能导致读取崩溃
	•	需要保护性解析与兼容逻辑
	4.	routes/tasks.py 中 result serialization、task summary、task detail、reviewer decision API、support-scope API 必须统一 contract。
	5.	apps/web/src/types/control-plane.ts 必须与后端结构零漂移对齐。

必补测试
至少补强：
	•	apps/api/tests/test_task_service_and_routes.py
	•	store round-trip tests
	•	serialization compatibility tests

PR1 验收标准
	•	老任务可读
	•	新任务可写、可读、可回放
	•	canonical/legacy 无双真相
	•	route/store tests 全绿

⸻

PR2 — Source ingress / loader / runtime 同构（必须做到 100%）

目标
把 fixture 与 upload 两条 structured_review 入口彻底做成 contract 等价。

必改文件
	•	apps/api/src/routes/uploads.py
	•	apps/api/src/services/document_loader.py
	•	apps/api/src/orchestrator/deepresearch_runtime.py
	•	apps/web/src/components/home-dashboard.tsx

必做内容
	1.	上传单文件入口稳定化：
	•	MIME 校验
	•	provenance 一致性
	•	错误信息一致性
	•	fileType 差异不得静默吞掉
	2.	document_loader.py 必须统一 DOCX/PDF/MD/TXT 输出语义：
	•	parseMode
	•	parseWarnings
	•	parserLimited
	•	visibility 透传基础数据
	3.	deepresearch_runtime.py 中 structured_review 路径必须显式统一 fixture / upload 的 source resolver 逻辑。
	4.	home-dashboard.tsx 必须正确支持 fixture/upload 二选一，并提交完整 structured_review 参数。

必补测试
	•	apps/api/tests/test_runtime.py
	•	upload / fixture parity tests
	•	ingress/runtime integration tests

PR2 验收标准
	•	fixture 与 upload 的结果 contract 等价
	•	上传文档可直接进入 structured_review
	•	无入口分叉真相

⸻

PR3 — L0 visibility / parser / manual-review artifact（必须做到 100%）

目标
把 L0 做成真正的第一层显式可验收产物，而不是隐式状态。

必改文件
	•	apps/api/src/review/parser/*
	•	apps/api/src/review/pipeline.py
	•	apps/web/src/components/task-detail.tsx
	•	apps/api/tests/test_structured_review.py

必做内容
	1.	parser 统一 reason codes：
	•	parseWarnings
	•	parserLimited
	•	附件状态原因码
	•	duplicate sections / attachment refs / visibility gap 的显式表达
	2.	PDF 必须继续保守表达 pdf_text_only，不得把未解析写成缺失。
	3.	pipeline 必须单独产出 L0 artifact，例如：
	•	visibility artifact
	•	parse diagnostics
	•	attachment visibility diagnostics
	4.	result / report / UI 中必须能明确区分：
	•	fully visible
	•	parser limited
	•	attachment_unparsed
	•	referenced_only
	•	missing
	•	unknown
	5.	task-detail.tsx 必须把 visibility panel 做成 reviewer 可读的信息面板，而不是 raw JSON 倾倒。

必补测试
	•	test_structured_review.py
	•	L0 artifact assertions
	•	parserLimited assertions
	•	attachment_unparsed/manual review assertions

PR3 验收标准
	•	L0 artifact 可读、可测、可展示
	•	PDF/DOCX/MD/TXT 语义一致
	•	未解析 ≠ 缺失

⸻

PR4 — Official scope 的 L1/L2 rules + evidence + report 加固（必须做到 100%）

目标
先把 official doc types 做硬，而不是到处撒规则。

必改文件
	•	apps/api/src/review/extractors/*
	•	apps/api/src/review/rules/engine.py
	•	apps/api/src/review/evidence/*
	•	apps/api/src/review/report/*

必做内容
	1.	优先深化 official doc types：
	•	construction_org
	•	hazardous_special_scheme
	2.	extractor 扩充但禁止 case-specific patch：
	•	section presence
	•	hazard tags
	•	resource/emergency facts
	•	unresolved facts taxonomy
	3.	rules/engine.py 必须加固 L1/L2：
	•	hard evidence
	•	visibility gap
	•	engineering inference
	•	suggestion enhancement 明确分层
	4.	evidence/* 必须把规范资产变成可计算资产：
	•	provenance
	•	clauseId
	•	applicability
	•	force level
	•	evidence-gap reason
	5.	report/* 必须显式拆分：
	•	hard defect
	•	visibility gap
	•	evidence gap
	•	enhancement

必补测试
	•	structured review unit tests
	•	official rules/evidence regression
	•	report semantics tests

PR4 验收标准
	•	official 两类文档 L1/L2 规则明显加硬
	•	issue / evidence / policy refs / unresolvedFacts 形成闭环
	•	report 不再混写 gap 与 defect

⸻

PR5 — support-scope / promotion / profile resolution 收口（必须做到 100%）

目标
把 official / experimental / skeleton / placeholder 的真相源彻底统一。

必改文件
	•	apps/api/src/review/support_scope.py
	•	apps/api/src/review/profile_resolver.py
	•	apps/api/src/review/rules/packs/*
	•	apps/web/src/components/structured-review-form.tsx
	•	docs/formal-review.md

必做内容
	1.	support_scope.py 成为唯一范围真相源。
	2.	明确 document type readiness 与 pack readiness 的关系。
	3.	ready pack 不得自动升格为 official。
	4.	promotion 必须有 entry criteria：
	•	rule coverage
	•	policy evidence
	•	tests
	•	versioned cases
	5.	profile_resolver.py 必须成为最终 resolvedProfile 的唯一裁决点，而不是口号式注释。
	6.	前端 form 必须清晰显示 official / experimental / skeleton / placeholder。

必补测试
	•	support-scope route tests
	•	profile resolution tests
	•	form / API assumption consistency tests

PR5 验收标准
	•	support-scope 不再多真相
	•	resolvedProfile 来源唯一
	•	UI 不误导成熟度

⸻

PR6 — Layered eval / stage gate 制度化（必须做到 100%）

目标
把分层评测变成真正的 merge gate，而不是 diagnostics 摆设。

必改文件
	•	apps/api/src/review/evaluation/*
	•	fixtures/review_eval/*
	•	docs/testing.md
	•	apps/api/tests/test_review_evaluation.py

必做内容
	1.	固化 official versioned stage gate 为主门槛。
	2.	experimental 只能进 diagnostics，不得阻断 official truth。
	3.	metrics 必须按 L0/L1/L2/L3 输出分组结果。
	4.	至少补齐下列层级指标映射：
	•	L0: visibility / parser / manual review
	•	L1: hard evidence / severity / recall
	•	L2: facts / rule hits / policy refs / unresolvedFacts
	•	L3: remediation bucket consistency / suggestion-vs-defect separation
	5.	完善 fixtures/review_eval/* 的 versioned cases/buckets。
	6.	让 make eval-review* 的输出能直接解释退化层。

必补测试
	•	test_review_evaluation.py
	•	grouped metrics tests
	•	stage gate tests
	•	official/experimental bucket behavior tests

PR6 验收标准
	•	layered eval 可运行、可解释、可阻断
	•	legacy baseline 不退化
	•	official gate 稳定

⸻

PR7 — Reviewer cockpit / decision writeback 闭环（必须做到 100%）

目标
让 reviewer workflow 真能用，不是只存个 JSON。

必改文件
	•	apps/api/src/review/reviewer_decision.py
	•	apps/api/src/services/task_service.py
	•	apps/api/src/routes/tasks.py
	•	apps/web/src/components/task-detail.tsx

必做内容
	1.	reviewer decision 的 task / issue / attachment 级状态机明确。
	2.	仅允许更新当前结果中真实存在的 issue/attachment id。
	3.	writeback 后：
	•	持久化成功
	•	再读取一致
	•	UI 展示一致
	•	report bucket 语义一致
	4.	task-detail 页面必须提供 reviewer 最小可用操作闭环，而不是只读展示。

必补测试
	•	reviewer decision route tests
	•	task_service writeback tests
	•	round-trip tests
	•	UI smoke / minimal integration tests

PR7 验收标准
	•	reviewer decision 可写回、可持久化、可读取、可展示
	•	无未知 id 污染
	•	task/issue/attachment 状态一致

⸻

PR8 — 文档统一归拢 + 发布前回归（必须做到 100%）

目标
让代码真相、文档真相、测试真相彻底对齐。

必改文件
	•	docs/architecture.md
	•	docs/formal-review.md
	•	docs/testing.md
	•	必要时 README.md

必做内容
	1.	文档只能陈述已经落地的代码事实。
	2.	说明：
	•	planner/router/profile_resolver 职责边界
	•	official / experimental / skeleton 边界
	•	L0/L1/L2/L3 语义
	•	reviewer workflow
	•	eval gates
	•	Gemini/seed 的使用边界
	3.	清理任何与当前实现不符的旧叙述。

必做回归
最终必须跑通并确认：
	•	make test
	•	make test-review-unit
	•	make test-review-integration
	•	make eval-review
	•	make eval-review-ablations
	•	make eval-review-cross-pack
	•	make eval-review-cross-model

PR8 验收标准
	•	docs 与代码一致
	•	全量回归通过
	•	无伪完成项

⸻

4. Definition of Done（硬门槛）

你只有在以下全部条件满足时，才算完成任务：
	1.	structured_review fixture / upload 两条路径 contract 等价
	2.	visibility 是唯一 canonical L0 contract
	3.	whetherManualReviewNeeded 仅作 legacy replay 兼容
	4.	official scope 的 L1/L2 regression 在 legacy baseline 与 official versioned stage gate 上都通过
	5.	facts_accuracy、rule_hit_accuracy、attachment_visibility_accuracy、manual_review_flag_accuracy 在官方门中稳定可见
	6.	reviewer decision 可写回、持久化、重读、展示
	7.	report/UI 明确分离 hard defect / visibility gap / evidence gap / suggestion enhancement
	8.	fixtures/construction 至少新增一层 derived clause/evidence manifests，且不污染原始规范文本
	9.	全量测试与评测命令通过
	10.	不存在 case-specific patch、环境路径污染、legacy alias 复活、文档先于代码等反模式 ￼

⸻

5. 输出要求

你完成后，必须按以下结构输出，不允许含糊：

A. 变更总览
	•	完成了哪些文件
	•	分别对应 PR1~PR8 哪一项
	•	为什么这些改动满足任务书要求

B. 分 PR 完成报告

对 PR1 到 PR8 分别给出：
	•	修改文件清单
	•	完成内容
	•	测试补充内容
	•	验收结论

C. 测试与评测结果

逐项列出执行结果：
	•	make test
	•	make test-review-unit
	•	make test-review-integration
	•	make eval-review
	•	make eval-review-ablations
	•	make eval-review-cross-pack
	•	make eval-review-cross-model

D. 风险与兼容性说明
	•	是否有 migration
	•	是否有 legacy 兼容处理
	•	是否有潜在回归点
	•	如何保证无双真相

E. 自我验收清单

逐条对照 G4 DoD 打勾，不许略过。

⸻

6. 你绝对不能做的烂事

以下任一出现，都视为失败：
	•	只改注释/文档/命名，不改核心逻辑
	•	只改一个层面，不补测试
	•	在 Makefile、脚本、代码里写死个人电脑路径
	•	在 rules/*、extractors/* 里按项目名/文件名/fixture id 写特判
	•	用更长 prompt 或更花的文风代替 contract / parser / eval 修复
	•	把 official / experimental / skeleton 混成一团
	•	把 ready packs 直接宣传成 official
	•	让 reviewer decision 存了但 UI 不可用
	•	让 docs 说的比代码多
	•	让 tests 绿不了还硬说“逻辑完成”

⸻

7. 最终执行口令

现在开始执行。
你的目标不是“尽量多改文件”，而是：

把 PR1-PR8 全量做完，并且每一层都有代码、测试、评测、文档、UI/API contract 对应闭环。

若某个点做不到，必须继续做直到做到；
不接受“先留 TODO”、“后续再补”、“这里只做个骨架”。

你交付的必须是可以进入严格验收的完整工程结果。

⸻
