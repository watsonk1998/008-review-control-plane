# Hermes 证据溯源重构 + 进度条校准 执行计划

## 1. 本次执行目标

完成阶段B剩余部分 + 阶段C全量实现 + 进度条行为校准。

## 2. 阶段B剩余任务（支持包边界收紧）

### 2.1 SupportPacketBuilder 实现 provenance_registry

**实现位置**: `apps/api/src/review/support_packet_builder.py`

- [ ] 在 `build_packet()` 中构造全局唯一 `provenance_registry: dict[str, EvidenceSpan]`
- [ ] 所有 EvidenceSpan 在进入注册表时生成稳定 span_id（复用 clause_store 算法）
- [ ] 实现 `_register_span()` 方法：去重、生成ID、返回引用
- [ ] 修改 `_load_basis_fulltext_context()`：不拷贝完整 span，只注册并返回 span_id
- [ ] 修改 `_build_spans()`：所有提取的文档 span 必须经过注册表
- [ ] 兼容存量数据：无 span_id 的对象自动生成并记录 warning

### 2.2 _collect_expert_review_points 门禁实现

- [ ] 增加开关参数 `validate_span_provenance: bool = True`
- [ ] 仅当 span_id 存在于 provenance_registry 时才收集为专家点
- [ ] 不存在的 span_id 降级为 `visibility_gap` 并标记幻觉风险
- [ ] 保留 fallback 模式用于调试

### 2.3 文档块 span_id 生成

**实现位置**: `apps/api/src/review/pipeline.py` / `_build_spans`

- [ ] 为每个提取的文档 Block/Table 生成确定性 span_id
- [ ] 绑定到对应的 EvidenceLocator
- [ ] 注册到 provenance_registry

## 3. 阶段C：输出端硬门实现

### 3.1 Assembler 校验层

**实现位置**: `apps/api/src/review/assembler.py`

- [ ] 遍历所有输出的 Issue，若引用 evidence_span_id 则校验存在性
- [ ] 校验失败降级为 `evidence_gap` 类型，标注 `[幻觉风险]`
- [ ] 保留原始 span 引用供调试
- [ ] 实现 `_validate_issue_provenance()` 校验方法

### 3.2 FinalReportRenderer 高亮标记

- [ ] 校验失败的 issue 在报告中显示红色高亮背景
- [ ] 增加 `hallucination_risk: bool` 字段到 ReviewIssue
- [ ] 报告中显示 span_id 校验状态
- [ ] 前端渲染时根据校验状态应用 `.highlight-red` 样式

### 3.3 审查视图跳转约定

- [ ] 锚点命名规范：`#evidence-span-{span_id}`
- [ ] 高亮颜色约定：
  - 🔴 红色：span_id 不存在 / 幻觉风险
  - 🟡 橙色：对象拷贝 / 无 provenance 引用
  - 🟢 绿色：正常溯源

## 4. 进度条行为校准（优先级最高）

### 4.1 前端进度条最终合同

**实现位置**: `apps/web/src/components/task-detail.tsx`

- [ ] ✅ 时间驱动为唯一主驱动：**6秒1%** 进度增长
- [ ] ✅ 上限 90%，任务未完成时永不超过
- [ ] ✅ 到达 90% 后完全停止增长，保持 90% 直到任务完成
- [ ] ✅ 任务完成后瞬时跳转到 100%
- [ ] ✅ stage 信号仅作为下界 floor，不做跳跃
- [ ] ✅ 禁止任何瞬跳行为，禁止进入页面时就有 18% 进度

### 4.2 后端 stage floor 约束

- `agent_running`: ≤25%
- `modules_running`: ≤75%
- `report_assembling`: ≤88%
- `finalize`: ≤95%

**验收标准**: 进入任何任务详情页，进度条从 0% 开始，每6秒涨1%，到90%停住，完成后到100%。

## 5. 实施顺序与验收点

| 序号 | 任务 | 预期交付时间 | 验收标准 |
|------|------|--------------|----------|
| 1 | 进度条校准 | 立即 | 进入页面从0开始，6秒1%，90%停住 |
| 2 | SupportPacketBuilder provenance_registry | 10分钟 | 注册表构建完成，无重复span |
| 3 | _collect_expert_review_points 校验 | 15分钟 | 无效span_id被拦截 |
| 4 | Assembler 校验层 | 20分钟 | 幻觉issue被降级标记 |
| 5 | 报告渲染高亮 | 25分钟 | 校验状态在报告中可见 |

## 6. 边界不变承诺

- ✅ 不修改 external/hermes-agent 任何代码
- ✅ 保持现有 API 100% 兼容
- ✅ 所有变更仅在控制面边界层
- ✅ 存量任务数据可正常加载

---

## 下一步确认

是否按此计划执行？
